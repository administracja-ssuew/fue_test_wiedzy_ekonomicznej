---
phase: 01-structural-refactor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - fue-quiz/package.json
  - fue-quiz/vite.config.js
  - fue-quiz/src/styles/global.css
  - fue-quiz/src/lib/gameLogic.js
autonomous: true
requirements:
  - STRUCT-01
must_haves:
  truths:
    - "Vitest and react-router-dom are present in package.json devDependencies/dependencies"
    - "fue-quiz/src/styles/global.css exists and contains all keyframe animations from the CSS string in App.jsx"
    - "fue-quiz/src/lib/gameLogic.js exports calcPts, cityInfo, getModule, moduleQuestions, ADMIN_CODE, ANSWER_BG, ANSWER_LABELS"
  artifacts:
    - path: "fue-quiz/src/styles/global.css"
      provides: "All global CSS rules and keyframe animations previously injected via useEffect"
      contains: "@keyframes su"
    - path: "fue-quiz/src/lib/gameLogic.js"
      provides: "Pure utility functions and UI constants extracted from App.jsx"
      exports: ["calcPts", "cityInfo", "getModule", "moduleQuestions", "ADMIN_CODE", "ANSWER_BG", "ANSWER_LABELS"]
  key_links:
    - from: "fue-quiz/src/lib/gameLogic.js"
      to: "fue-quiz/src/data/questions.js"
      via: "import { CITIES, MODULES, QUESTIONS } from '../data/questions.js'"
      pattern: "from.*data/questions"
---

<objective>
Set up the target directory structure and extract two self-contained artifacts from App.jsx that have zero dependencies on React state: the global CSS string and the pure utility functions (calcPts, cityInfo, getModule, moduleQuestions) plus their associated constants (ADMIN_CODE, ANSWER_BG, ANSWER_LABELS).

Also install Vitest and react-router-dom — both are locked decisions from STATE.md.

Purpose: Create the foundation all subsequent plans depend on. CSS and gameLogic have no React dependencies, so they can be extracted safely without touching any component code.
Output: package.json updated, styles/global.css, lib/gameLogic.js, directory scaffolding for screens/ and hooks/
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@c:/Users/Mikołaj/Downloads/fue-quiz-project/.planning/PROJECT.md
@c:/Users/Mikołaj/Downloads/fue-quiz-project/.planning/ROADMAP.md
@c:/Users/Mikołaj/Downloads/fue-quiz-project/.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Vitest, react-router-dom, and scaffold directory structure</name>
  <files>fue-quiz/package.json, fue-quiz/vite.config.js</files>
  <read_first>
    - fue-quiz/package.json — current dependencies to understand what is already present
    - fue-quiz/vite.config.js — current Vite config before modifying for Vitest
  </read_first>
  <action>
    Working directory for all commands: `fue-quiz/` subdirectory.

    1. Install dependencies:
       ```
       cd fue-quiz && npm install react-router-dom@^6.26.0
       npm install --save-dev vitest@^2.0.0 @testing-library/react@^16.0.0 @testing-library/jest-dom@^6.0.0 jsdom@^24.0.0
       ```

    2. Update vite.config.js to add Vitest test config. Add a `test` block to the existing config object:
       ```js
       test: {
         globals: true,
         environment: "jsdom",
         setupFiles: ["./src/test-setup.js"],
       }
       ```
       Also add `/// <reference types="vitest" />` as the first line of vite.config.js.

    3. Create `fue-quiz/src/test-setup.js`:
       ```js
       import "@testing-library/jest-dom";
       ```

    4. Add test script to package.json scripts:
       ```json
       "test": "vitest run",
       "test:watch": "vitest"
       ```

    5. Create empty directories (create a .gitkeep in each):
       - `fue-quiz/src/screens/`
       - `fue-quiz/src/hooks/`
       (styles/ and lib/ already exist or will be created by Task 2)

    Constraints:
    - Do NOT modify src/App.jsx in this task
    - Do NOT add TypeScript; stay pure JS
    - react-router-dom is a runtime dependency (not devDependency) — it will be used in App.jsx in Plan 04
  </action>
  <verify>
    <automated>cd "c:/Users/Mikołaj/Downloads/fue-quiz-project/fue-quiz" && node -e "const p=require('./package.json'); const ok=p.dependencies['react-router-dom']&&p.devDependencies['vitest']; console.log(ok?'PASS':'FAIL')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "react-router-dom" fue-quiz/package.json` exits 0
    - `grep -q "vitest" fue-quiz/package.json` exits 0
    - `grep -q "test" fue-quiz/vite.config.js` exits 0
    - `fue-quiz/src/test-setup.js` exists
    - `fue-quiz/src/screens/` directory exists
    - `fue-quiz/src/hooks/` directory exists
  </acceptance_criteria>
  <done>Vitest and react-router-dom installed; test infra wired; screens/ and hooks/ directories created</done>
</task>

<task type="auto">
  <name>Task 2: Extract global CSS to styles/global.css and gameLogic to lib/gameLogic.js</name>
  <files>
    fue-quiz/src/styles/global.css,
    fue-quiz/src/lib/gameLogic.js
  </files>
  <read_first>
    - fue-quiz/src/App.jsx lines 36-128 — the CSS constant and W object and utility functions to extract
    - fue-quiz/src/data/questions.js lines 1-15 — CITIES and MODULES exports that gameLogic.js will import
  </read_first>
  <action>
    DO NOT modify App.jsx yet. Only create new files.

    **Create `fue-quiz/src/styles/global.css`**

    Copy the entire CSS string from App.jsx lines 37-95 (the template literal assigned to `const CSS`) into this file as plain CSS. The file must contain:
    - The reset rules (`*, *::before, *::after`, `html, body`, `body`, `input button select textarea`, `button`)
    - All 9 @keyframes (su, pi, fi, bd, shake, conffall, glow, spin, pulse)
    - The animation class helpers (.su, .pi, .fi, .shake, .spinner)
    - The scrollbar rules
    - The layout utility classes (.fue-page, .fue-page-wide, .fue-quiz-layout, .fue-quiz-sidebar)
    - The .fue-input and .fue-tab rules
    - The .ans-btn rule
    - The @media (min-width: 900px) block with all responsive overrides

    Note: The Google Fonts link tag injection from the useEffect in App.jsx is NOT CSS — leave it in App.jsx for now; it will move to index.html in the thin-router plan (Plan 04).

    **Create `fue-quiz/src/lib/gameLogic.js`**

    Export the following, importing what is needed from questions.js:

    ```js
    import { CITIES, MODULES, QUESTIONS } from "../data/questions.js";

    export const ADMIN_CODE = "FUE2025";
    export const ANSWER_BG = ["#C2185B", "#1565C0", "#2E7D32", "#E65100"];
    export const ANSWER_LABELS = ["A", "B", "C", "D"];

    export const cityInfo = (n) => CITIES.find((c) => c.name === n) || { icon: "🎓", color: "#888" };
    export const calcPts = (timeLeft, maxTime, correct) =>
      correct ? Math.round(500 + (timeLeft / maxTime) * 500) : 0;
    export const getModule = (id) => MODULES.find((m) => m.id === id);
    export const moduleQuestions = (mod) => QUESTIONS.filter((q) => q.module === mod);
    ```

    This is an exact copy of the functions from App.jsx lines 16-20. Do not change the logic.
  </action>
  <verify>
    <automated>cd "c:/Users/Mikołaj/Downloads/fue-quiz-project/fue-quiz" && node --input-type=module &lt;&lt;&lt; "import { calcPts, cityInfo, getModule, moduleQuestions, ADMIN_CODE, ANSWER_BG, ANSWER_LABELS } from './src/lib/gameLogic.js'; console.log(calcPts(45, 90, true) === 750 ? 'PASS' : 'FAIL')"</automated>
  </verify>
  <acceptance_criteria>
    - `fue-quiz/src/styles/global.css` exists
    - `grep -c "@keyframes" fue-quiz/src/styles/global.css` returns 9 or more
    - `grep -q "conffall" fue-quiz/src/styles/global.css` exits 0
    - `grep -q "@media (min-width: 900px)" fue-quiz/src/styles/global.css` exits 0
    - `fue-quiz/src/lib/gameLogic.js` exists
    - `grep -q "export const calcPts" fue-quiz/src/lib/gameLogic.js` exits 0
    - `grep -q "export const ADMIN_CODE" fue-quiz/src/lib/gameLogic.js` exits 0
    - `grep -q "from.*data/questions" fue-quiz/src/lib/gameLogic.js` exits 0
    - App.jsx is NOT modified
  </acceptance_criteria>
  <done>global.css contains all CSS rules; gameLogic.js exports all utility functions and constants; App.jsx untouched</done>
</task>

</tasks>

<verification>
After both tasks:
1. `npm install` completes without errors in fue-quiz/
2. `grep "react-router-dom" fue-quiz/package.json` shows entry in dependencies
3. `grep "vitest" fue-quiz/package.json` shows entry in devDependencies
4. `fue-quiz/src/styles/global.css` has at least 80 lines
5. `fue-quiz/src/lib/gameLogic.js` exports calcPts, cityInfo, getModule, moduleQuestions, ADMIN_CODE, ANSWER_BG, ANSWER_LABELS
6. `npm run dev` still starts (App.jsx not yet changed — no regression possible)
</verification>

<success_criteria>
- react-router-dom and vitest are in package.json (locked decisions from STATE.md)
- styles/global.css is a standalone CSS file with all animations and layout rules
- lib/gameLogic.js is a module that can be imported and calcPts(45, 90, true) === 750
- App.jsx is completely unmodified (zero regression risk)
- screens/ and hooks/ directories exist ready for Plan 02 and 03
</success_criteria>

<output>
After completion, create `c:/Users/Mikołaj/Downloads/fue-quiz-project/.planning/phases/01-structural-refactor/01-01-SUMMARY.md`
</output>
