---
phase: 01-structural-refactor
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - fue-quiz/src/screens/Register.jsx
  - fue-quiz/src/screens/Login.jsx
  - fue-quiz/src/screens/AdminLogin.jsx
  - fue-quiz/src/screens/Practice.jsx
  - fue-quiz/src/screens/AdminPanel.jsx
  - fue-quiz/src/screens/Podium.jsx
autonomous: true
requirements:
  - STRUCT-01
  - STRUCT-06
must_haves:
  truths:
    - "Register.jsx, Login.jsx, AdminLogin.jsx, Practice.jsx, AdminPanel.jsx, Podium.jsx all exist under screens/"
    - "Each screen file is a standalone JSX module with named or default export"
    - "Each screen imports from lib/gameLogic.js (not from App.jsx) for constants and utilities"
    - "Each screen imports from lib/supabase.js and data/questions.js as needed"
  artifacts:
    - path: "fue-quiz/src/screens/Register.jsx"
      provides: "RegisterScreen component"
      exports: ["default RegisterScreen"]
    - path: "fue-quiz/src/screens/Login.jsx"
      provides: "LoginScreen component"
      exports: ["default LoginScreen"]
    - path: "fue-quiz/src/screens/AdminLogin.jsx"
      provides: "AdminLoginScreen component"
      exports: ["default AdminLoginScreen"]
    - path: "fue-quiz/src/screens/Practice.jsx"
      provides: "PracticeScreen component"
      exports: ["default PracticeScreen"]
    - path: "fue-quiz/src/screens/AdminPanel.jsx"
      provides: "AdminPanel and UserCard components"
      exports: ["default AdminPanel", "UserCard"]
    - path: "fue-quiz/src/screens/Podium.jsx"
      provides: "PodiumScreen component"
      exports: ["default PodiumScreen"]
  key_links:
    - from: "fue-quiz/src/screens/Register.jsx"
      to: "fue-quiz/src/lib/supabase.js"
      via: "import { registerUser } from '../lib/supabase.js'"
      pattern: "from.*lib/supabase"
    - from: "fue-quiz/src/screens/AdminPanel.jsx"
      to: "fue-quiz/src/lib/gameLogic.js"
      via: "import { cityInfo, ADMIN_CODE } from '../lib/gameLogic.js'"
      pattern: "from.*lib/gameLogic"
---

<objective>
Extract the six screen components that are already defined as standalone functions outside the App() function in App.jsx into their own files under screens/. These components require NO changes to their logic — only their import statements need updating to point at the new module locations (lib/gameLogic.js, lib/supabase.js, data/questions.js) instead of receiving everything from the same file.

Purpose: These screens are the lowest-risk extractions because they are already self-contained. After this plan, half the screens/ directory is populated.
Output: Register.jsx, Login.jsx, AdminLogin.jsx, Practice.jsx, AdminPanel.jsx (includes UserCard), Podium.jsx
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@c:/Users/Mikołaj/Downloads/fue-quiz-project/.planning/PROJECT.md
@c:/Users/Mikołaj/Downloads/fue-quiz-project/.planning/STATE.md
@c:/Users/Mikołaj/Downloads/fue-quiz-project/.planning/phases/01-structural-refactor/01-01-SUMMARY.md

<interfaces>
<!-- From fue-quiz/src/lib/gameLogic.js (created in Plan 01) -->
```js
export const ADMIN_CODE = "FUE2025";
export const ANSWER_BG = ["#C2185B", "#1565C0", "#2E7D32", "#E65100"];
export const ANSWER_LABELS = ["A", "B", "C", "D"];
export const cityInfo = (n) => CITIES.find((c) => c.name === n) || { icon: "🎓", color: "#888" };
export const calcPts = (timeLeft, maxTime, correct) => correct ? Math.round(500 + (timeLeft / maxTime) * 500) : 0;
export const getModule = (id) => MODULES.find((m) => m.id === id);
export const moduleQuestions = (mod) => QUESTIONS.filter((q) => q.module === mod);
```

<!-- From fue-quiz/src/lib/supabase.js (unchanged) -->
```js
export const DEMO;
export async function registerUser({ email, password, fullName, city, university });
export async function loginUser({ email, password });
export async function logoutUser();
export async function getCurrentUser();
export async function getPendingUsers(city);
export async function getAllUsers(city);
export async function verifyUser(userId, approve);
export async function createSession({ stage, city, createdBy });
export async function getActiveSession(city);
export async function updateSession(sessionId, updates);
export async function saveAttempt({ sessionId, userId, answers, totalScore, completed });
export async function getSessionAttempts(sessionId);
```

<!-- From fue-quiz/src/data/questions.js (unchanged) -->
```js
export const CITIES = [...];  // 5 city objects {name, icon, color}
export const MODULES = [...]; // 4 module objects {id, name, icon, color, timePerQ, desc}
export const QUESTIONS = [...]; // 32 question objects {id, module, q, opts, ans, exp}
export const PRACTICE_QUESTIONS = [...]; // practice subset
```

<!-- W object helper (remains in App.jsx for now — screens that use it keep using it inline) -->
<!-- Each screen currently uses W.back, W.btn, W.card, W.label, W.wrap, W.blob -->
<!-- Strategy: copy the W object definition into each screen file that needs it, OR inline the styles -->
<!-- DECISION: Copy full W object into each screen file to avoid touching logic. -->
<!-- W will be consolidated in Plan 04 when App.jsx is thinned. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract Register, Login, and AdminLogin screens</name>
  <files>
    fue-quiz/src/screens/Register.jsx,
    fue-quiz/src/screens/Login.jsx,
    fue-quiz/src/screens/AdminLogin.jsx
  </files>
  <read_first>
    - fue-quiz/src/App.jsx lines 754-889 — the three screen function definitions to copy verbatim
    - fue-quiz/src/App.jsx lines 98-128 — the W style helper object (must be included in each file)
  </read_first>
  <action>
    For each screen file, the structure is:

    ```
    [React imports needed by this component]
    [imports from lib/supabase.js]
    [imports from data/questions.js — only CITIES for Register]
    [imports from lib/gameLogic.js — ADMIN_CODE, DEMO for AdminLogin]
    [copy of W object — only the parts this component actually uses]
    [the function definition, copied verbatim from App.jsx]
    export default [FunctionName];
    ```

    **fue-quiz/src/screens/Register.jsx**
    - Import: `import { useState } from "react";`
    - Import: `import { registerUser } from "../lib/supabase.js";`
    - Import: `import { CITIES } from "../data/questions.js";`
    - Include W object (needs: W.back, W.btn, W.label, W.wrap, W.card — copy the full W definition)
    - Copy `RegisterScreen` function verbatim from App.jsx (lines 756-812)
    - Add: `export default RegisterScreen;`

    **fue-quiz/src/screens/Login.jsx**
    - Import: `import { useState } from "react";`
    - Import: `import { loginUser } from "../lib/supabase.js";`
    - Include W object (needs: W.back, W.btn, W.label, W.wrap)
    - Copy `LoginScreen` function verbatim from App.jsx (lines 816-855)
    - Add: `export default LoginScreen;`

    **fue-quiz/src/screens/AdminLogin.jsx**
    - Import: `import { useState } from "react";`
    - Import: `import { DEMO } from "../lib/supabase.js";`
    - Import: `import { ADMIN_CODE } from "../lib/gameLogic.js";`
    - Include W object (needs: W.back, W.btn, W.wrap)
    - Copy `AdminLoginScreen` function verbatim from App.jsx (lines 859-889)
    - Add: `export default AdminLoginScreen;`

    IMPORTANT: Copy W object exactly as it appears in App.jsx lines 98-128. The `W.back` function renders a JSX button — it needs React in scope, which is already imported via `import { useState } from "react"` (React 17+ JSX transform handles this).

    DO NOT modify any logic inside the screen functions. This is a pure extraction.
    DO NOT modify App.jsx in this task.
  </action>
  <verify>
    <automated>cd "c:/Users/Mikołaj/Downloads/fue-quiz-project/fue-quiz" && node --input-type=module &lt;&lt;&lt; "import RegisterScreen from './src/screens/Register.jsx'; console.log(typeof RegisterScreen === 'function' ? 'PASS' : 'FAIL')" 2>&1 | tail -1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "export default RegisterScreen" fue-quiz/src/screens/Register.jsx` exits 0
    - `grep -q "export default LoginScreen" fue-quiz/src/screens/Login.jsx` exits 0
    - `grep -q "export default AdminLoginScreen" fue-quiz/src/screens/AdminLogin.jsx` exits 0
    - `grep -q "from.*lib/supabase" fue-quiz/src/screens/Register.jsx` exits 0
    - `grep -q "from.*lib/gameLogic" fue-quiz/src/screens/AdminLogin.jsx` exits 0
    - `grep -q "ADMIN_CODE" fue-quiz/src/screens/AdminLogin.jsx` exits 0
    - App.jsx NOT modified
  </acceptance_criteria>
  <done>Three auth/registration screens extracted to screens/ with correct imports; App.jsx untouched</done>
</task>

<task type="auto">
  <name>Task 2: Extract Practice, AdminPanel (with UserCard), and Podium screens</name>
  <files>
    fue-quiz/src/screens/Practice.jsx,
    fue-quiz/src/screens/AdminPanel.jsx,
    fue-quiz/src/screens/Podium.jsx
  </files>
  <read_first>
    - fue-quiz/src/App.jsx lines 891-952 — PracticeScreen function
    - fue-quiz/src/App.jsx lines 954-1134 — AdminPanel and UserCard functions (both go in AdminPanel.jsx)
    - fue-quiz/src/App.jsx lines 1136-1205 — PodiumScreen function
    - fue-quiz/src/App.jsx lines 98-128 — W style helper object
  </read_first>
  <action>
    **fue-quiz/src/screens/Practice.jsx**
    - Import: `import { useState } from "react";`
    - Import: `import { PRACTICE_QUESTIONS, MODULES } from "../data/questions.js";`
    - Import: `import { ANSWER_LABELS } from "../lib/gameLogic.js";`
    - Include W object (needs: W.back, W.btn, W.card, W.wrap)
    - Copy `PracticeScreen` function verbatim from App.jsx (lines 893-952)
    - Add: `export default PracticeScreen;`

    **fue-quiz/src/screens/AdminPanel.jsx**
    - Import: `import { useState, useEffect } from "react";`
    - Import: `import { getAllUsers, verifyUser } from "../lib/supabase.js";`
    - Import: `import { CITIES, QUESTIONS, MODULES } from "../data/questions.js";`
    - Import: `import { cityInfo, moduleQuestions } from "../lib/gameLogic.js";`
    - Include W object (needs: W.btn, W.card, W.wrap)
    - Copy `AdminPanel` function verbatim (lines 956-1111)
    - Copy `UserCard` function verbatim (lines 1113-1134) — it is used inside AdminPanel
    - Export: `export default AdminPanel;` and `export { UserCard };`

    **fue-quiz/src/screens/Podium.jsx**
    - Import: `import { } from "react";` (no hooks needed — podStep/setPodStep come in as props)
    - Import: `import { cityInfo } from "../lib/gameLogic.js";`
    - Include W object if used (Podium does NOT use W — check the function; it uses inline styles directly)
    - Copy `PodiumScreen` function verbatim from App.jsx (lines 1138-1205)
    - Add: `export default PodiumScreen;`

    IMPORTANT for AdminPanel: The `AdminPanel` component receives `isDesktop` as a prop from App.jsx. This is correct — keep the prop interface unchanged. Do NOT introduce useWindowWidth inside AdminPanel.

    IMPORTANT for AdminPanel: `adminTab` state in the original App.jsx (line 136) is separate from the `tab` state defined inside AdminPanel itself. The `AdminPanel` component has its OWN `tab` state internally — it does not use the parent's `adminTab`. Copy the function as-is; the internal `tab` state is correct.

    DO NOT modify App.jsx.
  </action>
  <verify>
    <automated>cd "c:/Users/Mikołaj/Downloads/fue-quiz-project/fue-quiz" && node -e "const fs=require('fs'); ['Practice','AdminPanel','Podium'].map(f=>{ const exists=fs.existsSync('src/screens/'+f+'.jsx'); console.log(f+': '+(exists?'PASS':'FAIL')); })"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "export default PracticeScreen" fue-quiz/src/screens/Practice.jsx` exits 0
    - `grep -q "export default AdminPanel" fue-quiz/src/screens/AdminPanel.jsx` exits 0
    - `grep -q "export { UserCard }" fue-quiz/src/screens/AdminPanel.jsx` exits 0
    - `grep -q "export default PodiumScreen" fue-quiz/src/screens/Podium.jsx` exits 0
    - `grep -q "from.*lib/gameLogic" fue-quiz/src/screens/AdminPanel.jsx` exits 0
    - `grep -q "from.*data/questions" fue-quiz/src/screens/AdminPanel.jsx` exits 0
    - `grep -q "getAllUsers" fue-quiz/src/screens/AdminPanel.jsx` exits 0
    - App.jsx NOT modified
  </acceptance_criteria>
  <done>Practice, AdminPanel+UserCard, and Podium extracted to screens/; all imports point at correct modules; App.jsx untouched</done>
</task>

</tasks>

<verification>
After both tasks:
1. `ls fue-quiz/src/screens/` shows: Register.jsx, Login.jsx, AdminLogin.jsx, Practice.jsx, AdminPanel.jsx, Podium.jsx
2. Each file has a default export (grep "export default" across screens/)
3. No screen file imports from "../App.jsx" or from "./"
4. App.jsx line count unchanged (still ~1206 lines)
5. `npm run dev` still starts without errors (App.jsx not yet changed)
</verification>

<success_criteria>
- 6 screen files exist under screens/ each with a default export
- AdminPanel.jsx also exports UserCard as a named export
- All cross-module imports use correct relative paths (../lib/, ../data/)
- No screen imports from App.jsx
- App.jsx is completely unmodified — zero regression risk for DEMO mode (STRUCT-06)
</success_criteria>

<output>
After completion, create `c:/Users/Mikołaj/Downloads/fue-quiz-project/.planning/phases/01-structural-refactor/01-02-SUMMARY.md`
</output>
