---
phase: 06-rozgrywka-autorytatywna-serwera-i-plynnosc-jak-w-aplikacji
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/lib/plan.js
  - src/lib/plan.test.js
  - src/lib/plan.fixtures.json
  - src/lib/serverClock.js
  - src/lib/serverClock.test.js
autonomous: true
requirements: [P6-SC1, P6-SC2, P6-SC4, P6-SC5]

must_haves:
  truths:
    - "Dla dowolnej chwili i dowolnej pauzy faza (intro/countdown/quiz/reveal/paused/finished) i terminy wynikają wyłącznie z (items, anchorMs, pausedAtMs, nowMs)"
    - "Pauza w każdej fazie i wznowienie po 60 s daje tę samą fazę i ten sam pozostały czas"
    - "„Następne” skraca bieżące pytanie do teraz i przesuwa wszystkie kolejne terminy o tyle samo; drugi klik to no-op"
    - "Zmiana czasu modułu po zbudowaniu planu nie zmienia projekcji"
    - "Odpowiedź w strefie tolerancji po deadline ma czas = tpq (zero bonusu)"
    - "Offset zegara jest odporny na pojedynczą przekłamaną próbkę i przyjmuje próbki z odpowiedzi snapshotu"
    - "Sesja running/paused z kotwicą, ale bez wczytanego planu daje fazę plan_loading (nie legacy); legacy wyłącznie przy braku kotwicy"
  artifacts:
    - path: "src/lib/plan.js"
      provides: "Czyste funkcje planu — lustro SQL plan_position/sweep_decision"
      exports: ["FIRST_QUESTION_LEAD", "REVEAL_GATE_MS", "toMs", "buildPlanItems", "planPosition", "projectPlanState", "sweepDecision", "isRevealed", "resumeAnchor", "skipAnchor", "repeatAnchor", "answerResponseMs"]
    - path: "src/lib/plan.fixtures.json"
      provides: "Wspólne przypadki JS↔SQL (pozycja + zamiatacz)"
      contains: "\"position\""
    - path: "src/lib/plan.test.js"
      provides: "Vitest: fixture'y, pauza, przesunięcie, moduły, zamiatacz"
    - path: "src/lib/serverClock.js"
      provides: "computeOffset z filtrem pasma min-RTT + mediana, addClockSample, resync przy online"
      exports: ["serverNow", "computeOffset", "addClockSample", "syncServerClock", "startServerClock"]
  key_links:
    - from: "src/lib/plan.test.js"
      to: "src/lib/plan.fixtures.json"
      via: "import fixtures"
      pattern: "plan\\.fixtures\\.json"
    - from: "src/lib/plan.js"
      to: "src/lib/gameLogic.js"
      via: "import stałych REVEAL_SECONDS/MODULE_INTRO_SECONDS/PRE_QUESTION_LEAD"
      pattern: "from \"./gameLogic.js\""
---

<objective>
Zbudować czystą logikę planu sesji po stronie klienta (lustro funkcji SQL z sekcji 39) wraz ze wspólnymi fixture'ami JS↔SQL i testami Vitest, oraz wzmocnić `computeOffset` filtrem próbek (Claude's Discretion: „filtr próbek zegara w stylu timesync”).

Purpose: To jest specyfikacja wykonywalna całej fazy — SQL (06-02), hook uczestnika (06-04), LiveView/panel (06-06) i skrypt parzystości (06-03) implementują dokładnie te granice faz. Bez bazy, bez sieci, TDD.
Output: `src/lib/plan.js`, `src/lib/plan.fixtures.json`, `src/lib/plan.test.js`, zmodyfikowane `src/lib/serverClock.js` + test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-rozgrywka-autorytatywna-serwera-i-plynnosc-jak-w-aplikacji/06-CONTEXT.md
@.planning/phases/06-rozgrywka-autorytatywna-serwera-i-plynnosc-jak-w-aplikacji/06-RESEARCH.md
@src/lib/gameLogic.js
@src/lib/serverClock.js
@src/lib/serverClock.test.js

<interfaces>
Z src/lib/gameLogic.js (istniejące, NIE zmieniać):
```js
export const REVEAL_SECONDS = 6;
export const MODULE_INTRO_SECONDS = 30;
export const PRE_QUESTION_LEAD = 4;
```
Z src/lib/serverClock.js (istniejące):
```js
export function serverNow();            // Date.now() + offset
export function getClockOffset();
export function isClockSynced();
export function computeOffset(samples); // samples: [{ t0, t1, serverMs }]
export async function syncServerClock(rounds = 4);
export function startServerClock(refreshMs = 300000); // zwraca cleanup
```

KONTRAKT POZYCJI PLANU (identyczny z SQL `session_plans.items`, sekcja 39):
```js
// item = { i, id, m, tpq, lead, o, c, r }  — o/c/r w ms od kotwicy (anchor)
// lead: i===0 → 10 (FIRST_QUESTION_LEAD); m !== poprzedni m → 30 (MODULE_INTRO_SECONDS); inaczej 4 (PRE_QUESTION_LEAD)
// o = (i===0 ? 0 : r[i-1]) + lead*1000;  c = o + tpq*1000;  r = c + REVEAL_SECONDS*1000
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: plan.js + fixture'y + testy (planPosition, projectPlanState, sweepDecision, przesunięcia kotwicy)</name>
  <files>src/lib/plan.js, src/lib/plan.fixtures.json, src/lib/plan.test.js</files>
  <read_first>
    - src/lib/gameLogic.js (stałe czasu; styl komentarzy po polsku; projectLiveState jako wzorzec zwracanych pól)
    - src/lib/gameLogic.test.js (styl testów Vitest w repo: describe/it po polsku, import z ".js")
    - .planning/phases/06-rozgrywka-autorytatywna-serwera-i-plynnosc-jak-w-aplikacji/06-RESEARCH.md (Wzorzec 1, Wzorzec 3, Pułapka 3, 4, 5)
  </read_first>
  <behavior>
    - planPosition: fixture'y `position` (17+ przypadków) dają dokładnie oczekiwane idx/phase/opensAt/closesAt/revealUntil
    - sweepDecision: fixture'y `sweep` (10 przypadków) dają dokładnie oczekiwane action/status/idx/qStartedAtMs/revealedIdx
    - buildPlanItems([q1 m1, q2 m1, q3 m2], [{id:1,timePerQ:20},{id:2,timePerQ:30}]) === fixtures.items
    - "pauza": pauza w t=5000 (intro), 38000 (countdown), 15000 (quiz), 33000 (reveal), 80000 (intro modułu 2) → wznowienie po 60 s → ta sama phase i remainingMs co bez pauzy w tej samej chwili efektywnej
    - "przesunięcie": skip w t=15000 → reveal, closesAt===now, opens i1 = A+25000; drugi skip → null; skip z expectedIdx=1 przy idx 0 → null; repeat w t=15000 → opensAt===now, remainingMs=20000; answerResponseMs: 2345 / 20000 (tolerancja) / 20000 (null) / 0 (przed otwarciem)
    - "moduły": zmiana timePerQ w tablicy modułów po buildPlanItems nie zmienia projectPlanState; brak modułu → tpq 60
    - "zamiatacz": sekcja describe z fixture'ami sweep
    - "projectPlanState": fixture'y `project` — waiting→lobby; results→results; ended→ended; running bez kotwicy→legacy; running/paused z kotwicą i items null/[]→plan_loading; running z planem→faza jak planPosition
  </behavior>
  <action>
Utwórz `src/lib/plan.js` (ESM, podwójne cudzysłowy, 2 spacje, komentarze po polsku, import `import { REVEAL_SECONDS, MODULE_INTRO_SECONDS, PRE_QUESTION_LEAD } from "./gameLogic.js";`). Eksporty (dokładnie te nazwy):

```js
export const FIRST_QUESTION_LEAD = 10;   // zapowiedź modułu 1 (jak dzisiejszy start_quiz_session: +10 s)
export const REVEAL_GATE_MS = 1500;      // bramka odsłonięcia = closes + 1,5 s (strefa tolerancji submit)
export function toMs(ts)                 // null/undefined → null; number → number; string ISO → Date.parse(ts) (NaN → null)
export function buildPlanItems(questions, modules)
  // questions: [{ id, module }] JUŻ posortowane (module, sort_order, id); modules: [{ id, timePerQ }]
  // tpq = modules.find(m => m.id === q.module)?.timePerQ ?? 60; lead/o/c/r wg reguły z <interfaces>; zwraca nowe obiekty { i, id, m, tpq, lead, o, c, r }
export function planPosition(items, anchorMs, pausedAtMs, nowMs)
  // items puste/brak lub anchorMs == null → null
  // t = (pausedAtMs ?? nowMs) - anchorMs; last = items[items.length-1]; it = items.find(x => t < x.r) || last
  // phase: pausedAtMs != null → "paused"; t >= last.r → "finished"; t < it.o → (it.lead >= 10 ? "intro" : "countdown"); t < it.c → "quiz"; inaczej "reveal"
  // zwraca { idx: it.i, phase, item: it, opensAt: anchorMs + it.o, closesAt: anchorMs + it.c, revealUntil: anchorMs + it.r }
export function projectPlanState({ items, anchorMs, pausedAtMs = null, status, nowMs })
  // status "waiting" → { phase: "lobby" }; "results" → { phase: "results" }; "ended" → { phase: "ended" }
  // status running/paused i anchorMs == null → { phase: "legacy" } (sesja uruchomiona starym panelem, bez planu)
  // status running/paused, anchorMs != null, ale items puste/brak → { phase: "plan_loading" } — UPDATE z Realtime niesie
  //   kotwicę wcześniej, niż dociera snapshot z planem; bez tej fazy ~500 osób widziałoby na starcie komunikat „starsza wersja panelu”
  // efektywnie spauzowane gdy pausedAtMs != null LUB status === "paused" (wtedy pausedAt = pausedAtMs ?? nowMs)
  // pos = planPosition(items, anchorMs, null, effNow) gdzie effNow = pauza ? pausedAt : nowMs
  // remainingMs: intro/countdown → opensAt - effNow; quiz → closesAt - effNow; reveal → revealUntil - effNow; finished → 0
  // zwraca { phase: pauza ? "paused" : pos.phase, underPhase: pos.phase, idx, item, opensAt, closesAt, revealUntil,
  //          remainingMs, secondsLeft: Math.max(0, Math.ceil(remainingMs / 1000)), firstOfModule: item.lead >= 10 }
export function isRevealed(item, anchorMs, pausedAtMs, nowMs)   // ((pausedAtMs ?? nowMs) - anchorMs) >= item.c + REVEAL_GATE_MS
export function resumeAnchor(anchorMs, pausedAtMs, nowMs)       // anchorMs + (nowMs - pausedAtMs)
export function skipAnchor(items, anchorMs, nowMs, expectedIdx)  // pos = planPosition(items, anchorMs, null, nowMs); pos.idx !== expectedIdx || pos.phase !== "quiz" → null; inaczej anchorMs - (pos.closesAt - nowMs)
export function repeatAnchor(items, anchorMs, nowMs, expectedIdx)// ten sam warunek; anchorMs + (nowMs - pos.opensAt)
export function answerResponseMs(item, anchorMs, nowMs, chosen) // chosen == null → item.tpq*1000; inaczej min(max(0, nowMs - (anchorMs + item.o)), item.tpq*1000)
export function sweepDecision(row, items, nowMs)
  // row = { status, anchorMs, pausedAtMs, curIdx, qStartedAtMs, revealedIdx }
  // status !== "running" || anchorMs == null || pausedAtMs != null || !items?.length → { action: "none" }
  // pos = planPosition(items, anchorMs, null, nowMs); t = nowMs - anchorMs
  // rev = t >= pos.item.c + REVEAL_GATE_MS ? pos.idx : (pos.idx > 0 ? pos.idx - 1 : null)
  // pos.phase === "finished" → { action: "finish", status: "results", idx: pos.idx, qStartedAtMs: pos.opensAt, revealedIdx: pos.idx }
  // pos.idx !== row.curIdx || pos.opensAt !== row.qStartedAtMs || rev !== row.revealedIdx → { action: "update", status: "running", idx: pos.idx, qStartedAtMs: pos.opensAt, revealedIdx: rev }
  // inaczej { action: "none" }
```
Porównania null: `row.revealedIdx ?? null` vs `rev` (traktuj undefined jak null).

Utwórz `src/lib/plan.fixtures.json` (ten plik czyta też `scripts/verify-plan.js` z planu 06-03 — format jest kontraktem):
```json
{
  "anchorMs": 1790000000000,
  "questions": [{"id":"00000000-0000-0000-0000-000000000001","module":1},{"id":"00000000-0000-0000-0000-000000000002","module":1},{"id":"00000000-0000-0000-0000-000000000003","module":2}],
  "modules": [{"id":1,"timePerQ":20},{"id":2,"timePerQ":30}],
  "items": [
    {"i":0,"id":"00000000-0000-0000-0000-000000000001","m":1,"tpq":20,"lead":10,"o":10000,"c":30000,"r":36000},
    {"i":1,"id":"00000000-0000-0000-0000-000000000002","m":1,"tpq":20,"lead":4,"o":40000,"c":60000,"r":66000},
    {"i":2,"id":"00000000-0000-0000-0000-000000000003","m":2,"tpq":30,"lead":30,"o":96000,"c":126000,"r":132000}
  ],
  "position": [ { "name": "...", "t": <ms od kotwicy>, "pausedT": <ms od kotwicy lub null>, "expect": { "idx": n, "phase": "..." } } ],
  "sweep": [ { "name": "...", "t": <ms>, "row": { "status": "...", "curIdx": n|null, "qStartedT": <ms od kotwicy>|null, "revealedIdx": n|null, "pausedT": <ms>|null }, "expect": { "action": "...", "status"?: "...", "idx"?: n, "qStartedT"?: ms, "revealedIdx"?: n|null } } ],
  "project": [ { "name": "...", "status": "...", "hasAnchor": true|false, "items": "full"|"null"|"empty", "t": <ms od kotwicy>, "expect": { "phase": "...", "idx"?: n } } ]
}
```
Czasy w fixture'ach są WZGLĘDNE (`t`, `pausedT`, `qStartedT` = ms od `anchorMs`) — test i verify-plan dodają `anchorMs`. Oczekiwane opensAt/closesAt/revealUntil test liczy jako `anchorMs + items[expect.idx].o/c/r` (nie wpisuj ich do JSON).
Przypadki `position` (t → idx, phase): 0→0 intro; 9999→0 intro; 10000→0 quiz; 29999→0 quiz; 30000→0 reveal; 35999→0 reveal; 36000→1 countdown; 39999→1 countdown; 40000→1 quiz; 60000→1 reveal; 66000→2 intro; 95999→2 intro; 96000→2 quiz; 126000→2 reveal; 131999→2 reveal; 132000→2 finished; 900000→2 finished; pausedT=20000,t=80000→0 paused; pausedT=62000,t=62000→1 paused.
Przypadki `sweep` (status "running", pausedT null, o ile nie podano):
S1 cur 0, qStartedT 10000, rev null, t 20000 → none;
S2 cur 0, qStartedT 10000, rev null, t 31499 → none;
S3 cur 0, qStartedT 10000, rev null, t 31500 → update idx 0, qStartedT 10000, revealedIdx 0;
S4 cur 0, qStartedT 10000, rev 0, t 36000 → update idx 1, qStartedT 40000, revealedIdx 0;
S5 cur 1, qStartedT 40000, rev 0, t 50000 → none;
S6 cur 0, qStartedT 10000, rev null, t 70000 → update idx 2, qStartedT 96000, revealedIdx 1 (zaległość crona);
S7 cur 2, qStartedT 96000, rev 2, t 132000 → finish, status "results", idx 2, qStartedT 96000, revealedIdx 2;
S8 status "paused", pausedT 20000, cur 0, qStartedT 10000, rev null, t 50000 → none;
S9 status "waiting", cur 0, qStartedT null, rev null, t 1000 → none;
S10 cur 0, qStartedT null, rev null, t 1000 → update idx 0, qStartedT 10000, revealedIdx null.
Przypadki `project` (tylko JS — `scripts/verify-plan.js` z 06-03 czyta wyłącznie `position` i `sweep`, nowy klucz go nie psuje):
P1 waiting, hasAnchor false, items "null", t 0 → lobby;
P2 running, hasAnchor false, items "full", t 15000 → legacy;
P3 running, hasAnchor true, items "null", t 1000 → plan_loading;
P4 running, hasAnchor true, items "empty", t 1000 → plan_loading;
P5 paused, hasAnchor true, items "null", t 1000 → plan_loading;
P6 running, hasAnchor true, items "full", t 15000 → quiz, idx 0;
P7 results, hasAnchor true, items "full", t 200000 → results;
P8 ended, hasAnchor true, items "full", t 200000 → ended.

Utwórz `src/lib/plan.test.js` (Vitest, `import fixtures from "./plan.fixtures.json";`) z blokami `describe` o nazwach zawierających dokładnie: "planPosition", "zamiatacz", "pauza", "przesunięcie", "moduły", "projectPlanState" (filtry `-t` z 06-VALIDATION.md). Przypadki zgodnie z <behavior>. RED najpierw (commit `test(06-01): ...`), potem GREEN (`feat(06-01): ...`).
  </action>
  <verify>
    <automated>npx vitest run src/lib/plan.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/lib/plan.test.js` kończy się sukcesem, ≥ 35 asercji/testów zielonych
    - `npx vitest run src/lib/plan.test.js -t "pauza"`, `-t "przesunięcie"`, `-t "moduły"`, `-t "zamiatacz"` — każdy uruchamia ≥ 1 test i przechodzi
    - `grep -c "export function" src/lib/plan.js` ≥ 10; plik zawiera `export const REVEAL_GATE_MS = 1500` i `export const FIRST_QUESTION_LEAD = 10`
    - `src/lib/plan.fixtures.json` zawiera klucze `"anchorMs"`, `"items"`, `"position"`, `"sweep"`; `node -e "const f=require('./src/lib/plan.fixtures.json');if(f.position.length<19||f.sweep.length<10)process.exit(1)"` kończy się kodem 0
    - `grep -n "timePerQ" src/lib/plan.js` występuje wyłącznie w `buildPlanItems`
    - `grep -c "plan_loading" src/lib/plan.js` ≥ 1; `npx vitest run src/lib/plan.test.js -t "projectPlanState"` uruchamia ≥ 8 przypadków i przechodzi; `node -e "const f=require('./src/lib/plan.fixtures.json');if(!f.project||f.project.length<8)process.exit(1)"` kod 0
  </acceptance_criteria>
  <done>Czysta logika planu z pełnym pokryciem fixture'ami; fixture'y gotowe do testu parzystości z SQL.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: serverClock — filtr pasma min-RTT + mediana, addClockSample, resync przy online</name>
  <files>src/lib/serverClock.js, src/lib/serverClock.test.js</files>
  <read_first>
    - src/lib/serverClock.js (cały — zachować istniejące eksporty i zachowanie DEMO)
    - src/lib/serverClock.test.js (5 istniejących testów MUSI dalej przechodzić, w tym „wybiera próbkę o najmniejszym RTT”)
  </read_first>
  <behavior>
    - Istniejące 5 testów computeOffset przechodzi bez zmian
    - {rtt 20, off 1000}, {rtt 22, off 1002}, {rtt 25, off 5000} → 1002 (mediana w paśmie odrzuca przekłamaną próbkę)
    - {rtt 20, off 1000}, {rtt 24, off 1004} → 1002 (średnia dwóch środkowych przy parzystej liczbie)
    - addClockSample({t0:0,t1:20,serverMs:1010}) → getClockOffset() === 1000, isClockSynced() === true; bufor trzyma max 8 najnowszych próbek
  </behavior>
  <action>
Zmień `computeOffset(samples)` (zostaje czysta i eksportowana): odfiltruj próbki bez liczbowego `serverMs`; dla każdej `rtt = t1 - t0`, `off = serverMs - (t0 + t1) / 2`; `minRtt = min(rtt)`; zachowaj próbki z `rtt <= minRtt * 1.5 + 10` (pasmo najlepszych próbek); wynik = mediana `off` zachowanych (parzysta liczba → średnia dwóch środkowych), `Math.round`. Pusta lista → 0. Komentarz po polsku: dlaczego pasmo min-RTT zamiast „mediana ± odchylenie” (to drugie łamie test „wybiera próbkę o najmniejszym RTT” — rozrzut RTT 20/600/1000 zatrzymałby wszystkie próbki).

Dodaj moduł-level `let samples = [];` (bufor, max 8) oraz:
```js
export function addClockSample(sample) // { t0, t1, serverMs } — ignoruj gdy !Number.isFinite(sample?.serverMs); push, przytnij do 8 najnowszych, offset = computeOffset(samples), synced = true
```
`syncServerClock(rounds = 6)` (domyślnie 6 zamiast 4): zbiera świeże próbki, a jeśli ≥ 1 → `samples = fresh.slice(-8)`, `offset = computeOffset(samples)`, `synced = true`. DEMO bez zmian (offset 0).
`startServerClock`: dodatkowo `window.addEventListener("online", onOnline)` gdzie `onOnline = () => syncServerClock()`; cleanup usuwa listener. Zachowaj istniejące `visibilitychange`.
Dopisz testy z <behavior> do `src/lib/serverClock.test.js` (import `addClockSample, getClockOffset, isClockSynced`).
  </action>
  <verify>
    <automated>npx vitest run src/lib/serverClock.test.js src/lib/plan.test.js src/lib/gameLogic.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/lib/serverClock.test.js` zielone, ≥ 8 testów (5 starych + ≥ 3 nowe)
    - `grep -n "export function addClockSample" src/lib/serverClock.js` zwraca 1 linię
    - `grep -n "rounds = 6" src/lib/serverClock.js` zwraca 1 linię
    - `grep -n "\"online\"" src/lib/serverClock.js` zwraca ≥ 2 linie (add + remove)
    - `npm test` zielone w całości
  </acceptance_criteria>
  <done>Zegar przyjmuje próbki ze snapshotu i jest odporny na pojedynczą złą próbkę; stare testy nietknięte.</done>
</task>

</tasks>

<verification>
`npm test` — całość zielona (58 dotychczasowych + nowe). Brak zmian w kodzie UI.
</verification>

<success_criteria>
- plan.js eksportuje pełny kontrakt z <interfaces>; fixture'y JS przechodzą w Vitest
- serverClock ma `addClockSample` i filtr pasma; stare testy zielone
</success_criteria>

<output>
After completion, create `.planning/phases/06-rozgrywka-autorytatywna-serwera-i-plynnosc-jak-w-aplikacji/06-01-SUMMARY.md`
</output>
