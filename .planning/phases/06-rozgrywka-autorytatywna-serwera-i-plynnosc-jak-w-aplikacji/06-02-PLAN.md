---
phase: 06-rozgrywka-autorytatywna-serwera-i-plynnosc-jak-w-aplikacji
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - SUPABASE_FIXES.sql
autonomous: true
requirements: [P6-SC1, P6-SC4, P6-SC5, P6-SC6]

must_haves:
  truths:
    - "Start sesji v2 zamraża kolejność pytań i czas każdego pytania w session_plans.items"
    - "Zamiatacz advance_due_sessions() wyprowadza stan z kotwicy i zegara (idempotentny), pisze stary kontrakt current_question_idx/q_started_at i ustawia status='results' po ostatnim reveal"
    - "submit_answer_v2 wyznacza aktywne pytanie z planu i clock_timestamp(), nie zwraca poprawności"
    - "Snapshot i podsumowanie v2 zwracają poprawność dopiero po closes_at + 1,5 s czasu efektywnego"
    - "Wszystkie zmiany sekcji 39–40 są addytywne: nowe obiekty lub kompatybilne sygnatury; sesje bez planu działają po staremu"
    - "Snapshot z przypiętym id zakończonej sesji (results/ended) zwraca nowszą nie-zakończoną sesję miasta, jeśli taka istnieje"
  artifacts:
    - path: "SUPABASE_FIXES.sql"
      provides: "Sekcja 39 (plan, v2 RPC, zamiatacz) i 40 (pg_cron + sweeper_status)"
      contains: "─── 39."
  key_links:
    - from: "advance_due_sessions"
      to: "sweep_decision / plan_position"
      via: "SELECT * INTO d FROM public.sweep_decision(...)"
      pattern: "sweep_decision\\("
    - from: "cron.schedule('fue-advance-due'"
      to: "public.advance_due_sessions()"
      via: "pg_cron co 1 s"
      pattern: "fue-advance-due"
---

<objective>
Dopisać do `SUPABASE_FIXES.sql` sekcję 39 (plan sesji, jedna funkcja pozycji, decyzja zamiatacza, start/submit/snapshot/summary v2, akcje admina v2, zamiatacz, łatki kompatybilności starych RPC) i sekcję 40 (pg_cron + diagnostyka). Tylko tekst SQL — wgranie na produkcję jest krokiem ręcznym w planie 06-03 (per CONTEXT: „Wgrywanie SQL tylko ręcznie w SQL Editor”).

Purpose: Przejścia wykonuje baza (per CONTEXT „Przejścia”), punkty czystym zegarem serwera, poprawna odpowiedź po deadline (SC5), migracja addytywna (SC6).
Output: Nowe sekcje 39 i 40 na końcu `SUPABASE_FIXES.sql` (przed stopką „Done.”).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-rozgrywka-autorytatywna-serwera-i-plynnosc-jak-w-aplikacji/06-CONTEXT.md
@.planning/phases/06-rozgrywka-autorytatywna-serwera-i-plynnosc-jak-w-aplikacji/06-RESEARCH.md

<interfaces>
Istniejące na produkcji (NIE zmieniać sygnatur):
- `public.get_my_role() RETURNS TEXT` (NULL dla anona/bez profilu)
- `public.code_exists(p_code TEXT) RETURNS BOOLEAN`
- `public.start_quiz_session(p_session_id UUID) RETURNS TIMESTAMPTZ` (§29.5, linie 836–846)
- `public.advance_session_question(p_session_id UUID, p_expected_idx INT, p_next_idx INT, p_lead_seconds INT DEFAULT 4) RETURNS TIMESTAMPTZ` (§26, GRANT anon)
- `public.get_admin_answer_summary(p_session_id UUID, p_question_id UUID) RETURNS JSON` (§29.4)
- Tabele: `quiz_sessions(id, city, status CHECK IN ('waiting','running','paused','results','ended'), current_question_idx INT NOT NULL, q_started_at, is_practice, bg, bg_mobile, pause_elapsed_s INT, name, created_at, ...)` z `REPLICA IDENTITY FULL`; `answers(session_id, participant_code, participant_name, city, question_id, module, chosen, is_correct, points, response_time_s, response_time_ms, UNIQUE(session_id, participant_code, question_id))`; `questions(id, city, module, q, opts TEXT[], ans INT, is_practice, sort_order)`; `modules(id, time_per_q)`; `participant_codes(code, city, session_id, used)`.

Kontrakt pozycji planu (identyczny z src/lib/plan.js z 06-01):
`items` = jsonb array `[{ "i", "id", "m", "tpq", "lead", "o", "c", "r" }]`, kolejność `ORDER BY module, sort_order, id`, `lead`: i=0 → 10, zmiana modułu → 30, inaczej 4; `o = (i=0 ? 0 : r[i-1]) + lead*1000`; `c = o + tpq*1000`; `r = c + 6000`. Fazy: `paused` gdy pauza; `finished` gdy t ≥ r[last]; t < o → `intro` (lead ≥ 10) / `countdown`; t < c → `quiz`; inaczej `reveal`. Bramka odsłonięcia: t_eff ≥ c + 1500.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sekcja 39.1–39.6 — tabela planu, kolumny, plan_position, sweep_decision, build_session_plan, start v2, submit v2, snapshot, summary v2</name>
  <files>SUPABASE_FIXES.sql</files>
  <read_first>
    - SUPABASE_FIXES.sql (linie 589–636 §26, 754–846 §29, 1062–1111 §36, 1221–1287 §38 i stopka — konwencja nagłówków „-- ─── NN. …”, GRANT/REVOKE, komentarze po polsku)
    - SUPABASE_SCHEMA.sql (linie 148–192: quiz_sessions, answers)
    - (NIE czytaj src/lib/plan.js — powstaje równolegle w 06-01 w tej samej fali; kontrakt granic faz jest w <interfaces> powyżej)
  </read_first>
  <action>
Wstaw PRZED linią `-- ════…  Done. Verify…` nowy blok, nagłówek:
`-- ─── 39. PLAN SESJI + ZAMIATACZ — rozgrywka autorytatywna serwera (faza 6) ──`
z komentarzem: wyłącznie addytywne; sesje bez planu (`plan_anchor_at IS NULL`) działają po staremu; wgrywać ręcznie w SQL Editorze projektu `ytbwmmqwbfcugouourih`.

39.1 Tabela + kolumny (dosłownie):
```sql
CREATE TABLE IF NOT EXISTS public.session_plans (
  session_id UUID PRIMARY KEY REFERENCES public.quiz_sessions(id) ON DELETE CASCADE,
  items      JSONB NOT NULL,
  created_at TIMESTAMPTZ NOT NULL DEFAULT clock_timestamp()
);
ALTER TABLE public.session_plans ENABLE ROW LEVEL SECURITY;
DROP POLICY IF EXISTS "session_plans_read" ON public.session_plans;
CREATE POLICY "session_plans_read" ON public.session_plans FOR SELECT USING (true);
REVOKE ALL ON public.session_plans FROM anon, authenticated;
GRANT SELECT ON public.session_plans TO anon, authenticated;
ALTER TABLE public.quiz_sessions ADD COLUMN IF NOT EXISTS plan_anchor_at TIMESTAMPTZ;
ALTER TABLE public.quiz_sessions ADD COLUMN IF NOT EXISTS plan_paused_at TIMESTAMPTZ;
ALTER TABLE public.quiz_sessions ADD COLUMN IF NOT EXISTS revealed_idx INT;
ALTER TABLE public.quiz_sessions ADD COLUMN IF NOT EXISTS revealed_ans INT;
```
Komentarz: plan w osobnej tabeli, bo `REPLICA IDENTITY FULL` wysyłałby plan 2× w każdym zdarzeniu Realtime do ~500 subskrybentów; items nie zawierają `ans`, więc SELECT dla anona jest bezpieczny.

39.2 `plan_position` (LANGUAGE sql STABLE, bez dostępu do tabel):
```sql
CREATE OR REPLACE FUNCTION public.plan_position(
  p_items JSONB, p_anchor TIMESTAMPTZ, p_paused_at TIMESTAMPTZ, p_at TIMESTAMPTZ)
RETURNS TABLE (idx INT, phase TEXT, opens_at TIMESTAMPTZ, closes_at TIMESTAMPTZ,
               reveal_until TIMESTAMPTZ, question_id UUID, tpq INT)
LANGUAGE sql STABLE SET search_path = public AS $$
  WITH t AS (SELECT EXTRACT(epoch FROM (COALESCE(p_paused_at, p_at) - p_anchor)) * 1000 AS ms),
  it AS (
    SELECT (e->>'i')::INT AS i, (e->>'id')::UUID AS id, (e->>'tpq')::INT AS tpq, (e->>'lead')::INT AS lead,
           (e->>'o')::BIGINT AS o, (e->>'c')::BIGINT AS c, (e->>'r')::BIGINT AS r
    FROM jsonb_array_elements(COALESCE(p_items, '[]'::jsonb)) e
  ),
  cur AS (SELECT it.* FROM it, t WHERE t.ms < it.r ORDER BY it.i LIMIT 1),
  lst AS (SELECT it.* FROM it ORDER BY it.i DESC LIMIT 1),
  x   AS (SELECT * FROM cur UNION ALL SELECT * FROM lst WHERE NOT EXISTS (SELECT 1 FROM cur))
  SELECT x.i,
         CASE WHEN p_paused_at IS NOT NULL THEN 'paused'
              WHEN t.ms >= (SELECT r FROM lst) THEN 'finished'
              WHEN t.ms <  x.o THEN CASE WHEN x.lead >= 10 THEN 'intro' ELSE 'countdown' END
              WHEN t.ms <  x.c THEN 'quiz'
              ELSE 'reveal' END,
         p_anchor + x.o * INTERVAL '1 millisecond',
         p_anchor + x.c * INTERVAL '1 millisecond',
         p_anchor + x.r * INTERVAL '1 millisecond',
         x.id, x.tpq
  FROM x, t;
$$;
REVOKE EXECUTE ON FUNCTION public.plan_position(JSONB, TIMESTAMPTZ, TIMESTAMPTZ, TIMESTAMPTZ) FROM PUBLIC;
GRANT  EXECUTE ON FUNCTION public.plan_position(JSONB, TIMESTAMPTZ, TIMESTAMPTZ, TIMESTAMPTZ) TO anon, authenticated;
```
Komentarz: GRANT anon umożliwia test parzystości JS↔SQL (`npm run verify-plan`) — funkcja nie czyta tabel.

39.3 `sweep_decision` (plpgsql STABLE, GRANT anon, lustro `sweepDecision` z plan.js):
sygnatura `(p_items JSONB, p_anchor TIMESTAMPTZ, p_paused_at TIMESTAMPTZ, p_status TEXT, p_cur_idx INT, p_q_started TIMESTAMPTZ, p_revealed_idx INT, p_at TIMESTAMPTZ) RETURNS TABLE (action TEXT, new_status TEXT, new_idx INT, new_q_started_at TIMESTAMPTZ, new_revealed_idx INT)`. Ciało: gdy `p_status IS DISTINCT FROM 'running' OR p_anchor IS NULL OR p_paused_at IS NOT NULL OR p_items IS NULL OR jsonb_array_length(p_items) = 0` → jeden wiersz `('none', NULL, NULL, NULL, NULL)`. Inaczej `SELECT * INTO p FROM public.plan_position(p_items, p_anchor, NULL, p_at)`; `v_t := EXTRACT(epoch FROM (p_at - p_anchor)) * 1000`; `v_c := (p_items->p.idx->>'c')::BIGINT`; `v_rev := CASE WHEN v_t >= v_c + 1500 THEN p.idx WHEN p.idx > 0 THEN p.idx - 1 ELSE NULL END`; `p.phase = 'finished'` → `('finish', 'results', p.idx, p.opens_at, p.idx)`; `p.idx IS DISTINCT FROM p_cur_idx OR p.opens_at IS DISTINCT FROM p_q_started OR v_rev IS DISTINCT FROM p_revealed_idx` → `('update', 'running', p.idx, p.opens_at, v_rev)`; inaczej `('none', NULL, NULL, NULL, NULL)`. Używaj `RETURN QUERY SELECT ...; RETURN;` z jawnymi rzutowaniami `::TEXT/::INT/::TIMESTAMPTZ`.

39.4 `build_session_plan(p_city TEXT) RETURNS JSONB` — plpgsql STABLE SECURITY DEFINER; pętla `FOR r IN SELECT q.id, q.module, COALESCE(m.time_per_q, 60) AS tpq FROM public.questions q LEFT JOIN public.modules m ON m.id = q.module WHERE q.city = p_city AND q.is_practice = false ORDER BY q.module, q.sort_order, q.id`; `v_lead := CASE WHEN v_i = 0 THEN 10 WHEN r.module IS DISTINCT FROM v_prev_m THEN 30 ELSE 4 END`; `v_o := v_prev_r + v_lead*1000; v_c := v_o + r.tpq*1000; v_r := v_c + 6000`; dopisz `jsonb_build_object('i',v_i,'id',r.id,'m',r.module,'tpq',r.tpq,'lead',v_lead,'o',v_o,'c',v_c,'r',v_r)`; zwróć tablicę (pusta `'[]'` gdy brak pytań). `REVOKE EXECUTE ... FROM PUBLIC, anon, authenticated;`. Komentarz: stałe 10/30/4/6 MUSZĄ być zgodne z FIRST_QUESTION_LEAD/MODULE_INTRO_SECONDS/PRE_QUESTION_LEAD/REVEAL_SECONDS (Pułapka 5).

39.5 `start_quiz_session_v2(p_session_id UUID) RETURNS JSON` — SECURITY DEFINER: `v_now := clock_timestamp()`; `IF COALESCE(public.get_my_role(), '') NOT IN ('city_admin','superadmin') THEN RAISE EXCEPTION 'forbidden'; END IF;` (COALESCE: stary wzorzec `get_my_role() NOT IN` przepuszcza NULL); `SELECT city, status ... FOR UPDATE`; brak → `{ok:false, reason:'not found'}`; status ≠ 'waiting' → `{ok:false, reason:'not waiting', session: row_to_json}`; `v_items := public.build_session_plan(v_city)`; pusty → `{ok:false, reason:'no questions'}`; `INSERT INTO public.session_plans (session_id, items) VALUES (...) ON CONFLICT (session_id) DO UPDATE SET items = EXCLUDED.items, created_at = clock_timestamp()`; `UPDATE quiz_sessions SET status='running', current_question_idx=0, plan_anchor_at=v_now, plan_paused_at=NULL, revealed_idx=NULL, revealed_ans=NULL, pause_elapsed_s=NULL, q_started_at = v_now + ((v_items->0->>'o')::BIGINT) * INTERVAL '1 millisecond'`; zwróć `json_build_object('ok', true, 'reason', NULL, 'items', jsonb_array_length(v_items), 'session', (SELECT row_to_json(s) FROM public.quiz_sessions s WHERE s.id = p_session_id))`. `REVOKE ... FROM PUBLIC, anon; GRANT ... TO authenticated;`.

39.6a `submit_answer_v2(p_session_id UUID, p_code TEXT, p_name TEXT, p_question_id UUID, p_chosen INT) RETURNS JSON` — SECURITY DEFINER, `v_now := clock_timestamp()` RAZ na początku (Pułapka 6). Kolejno: `code_exists` → 'invalid code'; `p_chosen` poza NULL/0..3 → 'invalid choice'; `v_name := left(COALESCE(p_name,''),120)`; `SELECT s.status, s.plan_anchor_at, s.plan_paused_at, sp.items FROM quiz_sessions s JOIN session_plans sp ON sp.session_id = s.id WHERE s.id = p_session_id` — brak lub `plan_anchor_at IS NULL` → 'session has no plan'; pauza (`plan_paused_at IS NOT NULL OR status='paused'`) → 'session paused'; status ≠ 'running' → 'session not running'; pozycja pytania z planu po `id` (`SELECT e INTO v_item FROM jsonb_array_elements(v_items) e WHERE (e->>'id')::UUID = p_question_id LIMIT 1`), brak → 'question not in plan'; `v_opens := anchor + o ms`, `v_closes := anchor + c ms`; `v_now < v_opens` → 'question not started'; `p_chosen IS NOT NULL AND v_now > v_closes + INTERVAL '1.5 seconds'` → 'time is up'; `v_rt_ms := CASE WHEN p_chosen IS NULL THEN v_tpq*1000 ELSE LEAST(GREATEST(0, EXTRACT(epoch FROM (v_now - v_opens))*1000), v_tpq*1000)::INT END` (strefa tolerancji = tpq, zero bonusu — decyzja „Punktacja i uczciwość”); `is_correct` liczone jak w §36; `INSERT ... (points=0, response_time_s = v_rt_ms/1000, response_time_ms = v_rt_ms) ON CONFLICT (session_id, participant_code, question_id) DO NOTHING; GET DIAGNOSTICS v_rows = ROW_COUNT;` duplikat → `{accepted:true, duplicate:true, chosen:<zapisany chosen>}`; inaczej `{accepted:true, duplicate:false, chosen:p_chosen}`. ZERO pól `is_correct`/`correct_ans` w odpowiedzi. GRANT anon, authenticated.

39.6b `get_participant_state(p_code TEXT, p_session_id UUID DEFAULT NULL, p_include_plan BOOLEAN DEFAULT true) RETURNS JSON` — plpgsql VOLATILE SECURITY DEFINER. Wszystkie znaczniki czasu w odpowiedzi jako epoch ms: `FLOOR(EXTRACT(epoch FROM x) * 1000)::BIGINT` (FLOOR = zgodność z `Date.parse`, które obcina do ms). Algorytm:
- `v_now := clock_timestamp()`; `v_code := upper(btrim(p_code))`; miasto kodu z `participant_codes`; brak → `{server_now, error:'invalid code'}`.
- sesja: jeśli `p_session_id` podane → `WHERE id = p_session_id AND city = v_city` (zmienna `v_found BOOLEAN` z `FOUND` — nie odwołuj się do pól nieprzypisanego RECORD). **Porzucenie nieaktualnej przypiętej sesji:** gdy znaleziona sesja ma `status IN ('results','ended')` AND `EXISTS (SELECT 1 FROM public.quiz_sessions n WHERE n.city = v_city AND n.status <> 'ended' AND n.created_at > <przypięta>.created_at)` → `v_found := false`. Komentarz po polsku: klient przypina `sessionId` w localStorage, a `endAndResetSession` tworzy NOWE id (próba → właściwy test, wcześniejsza próba generalna) — bez tej reguły uczestnik krąży w Lobby na starej sesji; zakończona przypięta sesja BEZ nowszej zostaje zwrócona (ekran wyników nie znika po 15-s siatce bezpieczeństwa). Gdy `NOT v_found` → `WHERE city = v_city AND status <> 'ended' ORDER BY CASE status WHEN 'running' THEN 0 WHEN 'paused' THEN 1 WHEN 'waiting' THEN 2 ELSE 3 END, created_at DESC LIMIT 1`; brak → `{server_now, error:NULL, session:NULL, position:NULL, plan:NULL, my_answers:[], reveal:NULL, correct_total:0}`.
- `v_items` z `session_plans` (tylko gdy `plan_anchor_at IS NOT NULL`); `v_t := EXTRACT(epoch FROM (COALESCE(plan_paused_at, v_now) - plan_anchor_at)) * 1000`; `v_final := status IN ('results','ended')`.
- `session` = `{id, city, status, is_practice, bg, bg_mobile, name, plan_anchor_at(ms), plan_paused_at(ms)}`.
- `position` (gdy plan) = `{idx, phase, opens_at, closes_at, reveal_until}` (ms) z `plan_position(v_items, anchor, paused, v_now)`.
- `plan` (gdy `p_include_plan` i plan): `SELECT json_agg(e || jsonb_build_object('q', COALESCE(q.q,'(pytanie usunięte)'), 'opts', COALESCE(to_jsonb(q.opts),'[]'::jsonb)) ORDER BY (e->>'i')::INT) FROM jsonb_array_elements(v_items) e LEFT JOIN public.questions q ON q.id = (e->>'id')::UUID` — BEZ `ans`.
- `my_answers` = `[{question_id, chosen, is_correct}]` dla (sesja, kod); `is_correct` = wartość tylko gdy `v_final OR (pozycja pytania w planie istnieje AND v_t >= c + 1500)`, inaczej NULL (LEFT JOIN LATERAL po `jsonb_array_elements(COALESCE(v_items,'[]'))`).
- `correct_total` = liczba `is_correct = true` wśród odsłoniętych (ten sam warunek).
- `reveal` = `{idx, ans}` dla najnowszego odsłoniętego: `v_rev := CASE WHEN v_final THEN jsonb_array_length(v_items)-1 WHEN v_t >= c[p.idx] + 1500 THEN p.idx WHEN p.idx > 0 THEN p.idx - 1 ELSE NULL END`; `ans` z `questions` po `items->v_rev->>'id'`; NULL gdy brak planu lub `v_rev IS NULL`.
- zwróć `{server_now, error:NULL, session, position, plan, my_answers, reveal, correct_total}`. GRANT anon, authenticated.

39.6c `get_answer_summary_v2(p_session_id UUID, p_question_id UUID) RETURNS JSON` — SECURITY DEFINER: sesja nie istnieje → `{total:0, correct:NULL, ans:NULL}`; `plan_anchor_at IS NULL` → `RETURN public.get_admin_answer_summary(p_session_id, p_question_id)`; `v_admin := COALESCE(public.get_my_role(),'') IN ('city_admin','superadmin')`; `v_open := v_admin OR status IN ('results','ended') OR (pozycja w planie AND t_eff >= c + 1500)` gdzie `t_eff` liczone od `COALESCE(plan_paused_at, clock_timestamp())`; `total` zawsze; `correct` i `ans` tylko gdy `v_open`. GRANT anon, authenticated.
  </action>
  <verify>
    <automated>grep -c "CREATE OR REPLACE FUNCTION public\.\(plan_position\|sweep_decision\|build_session_plan\|start_quiz_session_v2\|submit_answer_v2\|get_participant_state\|get_answer_summary_v2\)" SUPABASE_FIXES.sql</automated>
  </verify>
  <acceptance_criteria>
    - Powyższy grep zwraca 7
    - `grep -n "CREATE TABLE IF NOT EXISTS public.session_plans" SUPABASE_FIXES.sql` → 1 linia; `grep -c "ADD COLUMN IF NOT EXISTS \(plan_anchor_at\|plan_paused_at\|revealed_idx\|revealed_ans\)" SUPABASE_FIXES.sql` → 4
    - `grep -n "─── 39\." SUPABASE_FIXES.sql` → 1 linia, położona PO linii z `─── 38.` i PRZED `Done. Verify`
    - W ciele `submit_answer_v2` brak tekstu `correct_ans` i brak klucza `'is_correct'` w `json_build_object` odpowiedzi (sprawdź: `awk '/FUNCTION public.submit_answer_v2/,/END; \$\$;/' SUPABASE_FIXES.sql | grep -c "correct_ans"` → 0)
    - `grep -c "COALESCE(public.get_my_role(), '') NOT IN" SUPABASE_FIXES.sql` ≥ 1
    - `grep -n "interval '1.5 seconds'\|INTERVAL '1.5 seconds'" SUPABASE_FIXES.sql` obejmuje linię w submit_answer_v2; `grep -c "+ 1500" SUPABASE_FIXES.sql` ≥ 3 (sweep_decision, snapshot, summary v2)
    - `awk '/FUNCTION public.get_participant_state/,/END; \$\$;/' SUPABASE_FIXES.sql | grep -c "created_at >"` ≥ 1 (reguła porzucenia nieaktualnej przypiętej sesji)
    - Żadna istniejąca linia sekcji 1–38 nie została zmieniona ani usunięta (dotyczy też komentarzy `-- …`, których `grep "^-[^-]"` nie wykrywa): `git diff --numstat HEAD -- SUPABASE_FIXES.sql` → druga kolumna (usunięte linie) = 0 (tylko dopiski)
  </acceptance_criteria>
  <done>Sekcja 39.1–39.6 dopisana, czysto addytywna.</done>
</task>

<task type="auto">
  <name>Task 2: Sekcja 39.7–39.9 (akcje admina v2, zamiatacz, łatki kompatybilności) + sekcja 40 (pg_cron, sweeper_status)</name>
  <files>SUPABASE_FIXES.sql</files>
  <read_first>
    - SUPABASE_FIXES.sql (sekcja 39 z Task 1; §26 linie 595–636 i §29.5 linie 823–846 — oryginalne ciała advance_session_question i start_quiz_session)
    - .planning/phases/06-rozgrywka-autorytatywna-serwera-i-plynnosc-jak-w-aplikacji/06-RESEARCH.md (Wzorzec 1 tabela akcji, Wzorzec 2 harmonogram, Pułapka 3, 9)
  </read_first>
  <action>
Kontynuuj sekcję 39 (po 39.6c), potem osobny nagłówek 40.

39.7 Akcje admina — każda: `LANGUAGE plpgsql SECURITY DEFINER SET search_path = public`, `v_now TIMESTAMPTZ := clock_timestamp()`, `IF COALESCE(public.get_my_role(), '') NOT IN ('city_admin','superadmin') THEN RAISE EXCEPTION 'forbidden'; END IF;`, `SELECT * INTO s FROM public.quiz_sessions WHERE id = p_session_id FOR UPDATE;`, brak planu → `{ok:false, reason:'no plan'}`; wynik zawsze `json_build_object('ok', …, 'reason', …, 'session', (SELECT row_to_json(x) FROM public.quiz_sessions x WHERE x.id = p_session_id))`; `REVOKE EXECUTE ... FROM PUBLIC, anon; GRANT EXECUTE ... TO authenticated;`.
- `admin_pause_session(p_session_id UUID)`: wymaga `status='running' AND plan_paused_at IS NULL` (inaczej ok:false 'not running'); `UPDATE SET status='paused', plan_paused_at=v_now, pause_elapsed_s=FLOOR(EXTRACT(epoch FROM v_now))::INT` (pause_elapsed_s = zgodność ze starym frontem).
- `admin_resume_session(p_session_id UUID)`: wymaga `plan_paused_at IS NOT NULL` (inaczej 'not paused'); `v_anchor := s.plan_anchor_at + (v_now - s.plan_paused_at)`; `SELECT * INTO p FROM public.plan_position((SELECT items FROM public.session_plans WHERE session_id = p_session_id), v_anchor, NULL, v_now)`; `UPDATE SET status='running', plan_anchor_at=v_anchor, plan_paused_at=NULL, pause_elapsed_s=NULL, current_question_idx=p.idx, q_started_at=p.opens_at`.
- `admin_skip_question(p_session_id UUID, p_idx INT)`: wymaga running bez pauzy; pozycja z `plan_position(items, s.plan_anchor_at, NULL, v_now)`; `p.idx IS DISTINCT FROM p_idx OR p.phase <> 'quiz'` → `{ok:false, reason:'noop'}` (drugi klik / drugi admin = no-op); `v_delta := p.closes_at - v_now`; `UPDATE SET plan_anchor_at = s.plan_anchor_at - v_delta, q_started_at = p.opens_at - v_delta, current_question_idx = p.idx`. (Decyzja „⏭ Następne zostaje jako przesunięcie planu”.)
- `admin_repeat_question(p_session_id UUID, p_idx INT)`: ten sam warunek; `v_delta := v_now - p.opens_at`; `UPDATE SET plan_anchor_at = s.plan_anchor_at + v_delta, q_started_at = v_now, current_question_idx = p.idx`.

39.8 Zamiatacz:
```sql
CREATE OR REPLACE FUNCTION public.advance_due_sessions(p_only UUID DEFAULT NULL)
RETURNS INT LANGUAGE plpgsql SECURITY DEFINER SET search_path = public AS $$
DECLARE v_now TIMESTAMPTZ := clock_timestamp(); r RECORD; d RECORD; n INT := 0;
BEGIN
  FOR r IN
    SELECT s.id, s.status, s.current_question_idx, s.q_started_at, s.plan_anchor_at, s.plan_paused_at, s.revealed_idx, sp.items
    FROM public.quiz_sessions s JOIN public.session_plans sp ON sp.session_id = s.id
    WHERE s.status = 'running' AND s.plan_anchor_at IS NOT NULL AND s.plan_paused_at IS NULL
      AND (p_only IS NULL OR s.id = p_only)
    FOR UPDATE OF s SKIP LOCKED
  LOOP
    SELECT * INTO d FROM public.sweep_decision(r.items, r.plan_anchor_at, r.plan_paused_at, r.status,
                                                r.current_question_idx, r.q_started_at, r.revealed_idx, v_now);
    IF d.action = 'none' THEN CONTINUE; END IF;
    UPDATE public.quiz_sessions SET
      status = d.new_status, current_question_idx = d.new_idx, q_started_at = d.new_q_started_at,
      revealed_idx = d.new_revealed_idx,
      revealed_ans = CASE WHEN d.new_revealed_idx IS NULL THEN NULL
                          ELSE (SELECT q.ans FROM public.questions q WHERE q.id = (r.items->d.new_revealed_idx->>'id')::UUID) END
    WHERE id = r.id;
    n := n + 1;
  END LOOP;
  RETURN n;
END; $$;
REVOKE EXECUTE ON FUNCTION public.advance_due_sessions(UUID) FROM PUBLIC, anon, authenticated;
```
Komentarz: idempotentny (stan wyprowadzany z kotwicy, nie inkrementowany — anty-wzorzec `+= 1`), brak zapisu gdy nic się nie zmieniło, `SKIP LOCKED` nie czeka na akcję admina; `status='results'` po ostatnim reveal — decyzja „Koniec quizu” (podium dalej ręcznie).
`admin_sweep_session(p_session_id UUID) RETURNS INT` — rola (COALESCE) → `RETURN public.advance_due_sessions(p_session_id);`; GRANT authenticated (zapas, gdy pg_cron nie żyje — Pułapka 9).

39.9 Łatki kompatybilności (te same sygnatury, CREATE OR REPLACE, zachowanie dla sesji bez planu identyczne):
- `advance_session_question(UUID, INT, INT, INT DEFAULT 4)` — ciało jak w §26, WHERE rozszerzone o `AND plan_anchor_at IS NULL` (stary awaryjny kierowca i anon nie ruszą sesji z planem). Zachowaj `GRANT EXECUTE ... TO anon, authenticated`. NIE rób `DROP FUNCTION`.
- `start_quiz_session(UUID)` — ciało jak w §29.5, UPDATE dodatkowo `plan_anchor_at = NULL, plan_paused_at = NULL, revealed_idx = NULL, revealed_ans = NULL` (stary panel startujący wiersz z resztkami planu nie uruchomi zamiatacza). Zachowaj oryginalny warunek roli z §29.5 i `GRANT ... TO authenticated`.

Nagłówek `-- ─── 40. pg_cron — zamiatacz co 1 s + diagnostyka (faza 6) ──` z komentarzem: PRZED wgraniem w SQL Editorze uruchom `select version();` (sekundowe harmonogramy wymagają Postgres ≥ 15.1.1.61) i `select extversion from pg_extension where extname='pg_cron';`. Treść:
```sql
CREATE EXTENSION IF NOT EXISTS pg_cron WITH SCHEMA pg_catalog;
GRANT USAGE ON SCHEMA cron TO postgres;
GRANT ALL PRIVILEGES ON ALL TABLES IN SCHEMA cron TO postgres;
DO $do$ BEGIN
  PERFORM cron.unschedule(jobid) FROM cron.job WHERE jobname IN ('fue-advance-due', 'fue-cron-cleanup');
END $do$;
SELECT cron.schedule('fue-advance-due', '1 seconds', $cmd$SELECT public.advance_due_sessions()$cmd$);
SELECT cron.schedule('fue-cron-cleanup', '*/10 * * * *',
  $cmd$DELETE FROM cron.job_run_details WHERE end_time < now() - interval '1 hour'$cmd$);
```
Komentarz: `cron.log_run` nie da się wyłączyć bez superusera → „minimalne logowanie” = sprzątanie co 10 min (≈3600 wierszy/h).
`public.sweeper_status() RETURNS JSON` — plpgsql SECURITY DEFINER `SET search_path = public, pg_catalog`: brak rozszerzenia (`NOT EXISTS (SELECT 1 FROM pg_extension WHERE extname='pg_cron')`) → `{cron_installed:false, job_active:false, schedule:NULL, last_run_age_s:NULL, last_status:NULL}`; brak joba `fue-advance-due` → `{cron_installed:true, job_active:false, …}`; inaczej ostatni ZAKOŃCZONY wiersz `cron.job_run_details WHERE jobid = … AND end_time IS NOT NULL ORDER BY end_time DESC LIMIT 1` (komentarz: przebieg w toku ma `end_time IS NULL` — sortowanie po `start_time` dawałoby wiek NULL i fałszywe „zamiatacz nie żyje”) → `{cron_installed:true, job_active:<cron.job.active>, schedule, last_run_age_s: EXTRACT(epoch FROM (clock_timestamp() - end_time)), last_status: status}`; brak zakończonych przebiegów → `last_run_age_s:NULL, last_status:NULL`. `REVOKE ... FROM PUBLIC; GRANT ... TO anon, authenticated;` (zwraca tylko booleany/wiek — nic nie ujawnia).
  </action>
  <verify>
    <automated>grep -c "CREATE OR REPLACE FUNCTION public\.\(admin_pause_session\|admin_resume_session\|admin_skip_question\|admin_repeat_question\|advance_due_sessions\|admin_sweep_session\|sweeper_status\)" SUPABASE_FIXES.sql</automated>
  </verify>
  <acceptance_criteria>
    - Powyższy grep zwraca 7
    - `grep -n "FOR UPDATE OF s SKIP LOCKED" SUPABASE_FIXES.sql` → 1 linia
    - `grep -n "AND plan_anchor_at IS NULL" SUPABASE_FIXES.sql` → ≥ 1 linia w nowej definicji advance_session_question
    - `grep -n "'fue-advance-due', '1 seconds'" SUPABASE_FIXES.sql` → 1 linia; `grep -n "fue-cron-cleanup" SUPABASE_FIXES.sql` → ≥ 2 linie
    - `grep -n "─── 40\." SUPABASE_FIXES.sql` → 1 linia, przed stopką `Done. Verify`
    - `grep -n "REVOKE EXECUTE ON FUNCTION public.advance_due_sessions(UUID) FROM PUBLIC, anon, authenticated" SUPABASE_FIXES.sql` → 1 linia
    - `awk '/FUNCTION public.sweeper_status/,/END; \$\$;/' SUPABASE_FIXES.sql | grep -c "end_time IS NOT NULL"` ≥ 1
    - `git diff --numstat HEAD -- SUPABASE_FIXES.sql` → druga kolumna (usunięte linie) = 0 (wyłącznie dopiski; wykrywa też usunięte komentarze SQL)
    - Brak `DROP FUNCTION` w sekcjach 39–40: `awk '/─── 39\./,/Done. Verify/' SUPABASE_FIXES.sql | grep -c "DROP FUNCTION"` → 0
  </acceptance_criteria>
  <done>Sekcje 39 i 40 kompletne w repo, gotowe do ręcznego wgrania w 06-03.</done>
</task>

</tasks>

<verification>
Plik SQL tylko rozszerzony (`git diff --numstat` względem stanu sprzed planu: 0 usuniętych linii). Poprawność wykonania sprawdzi 06-03 (verify-prod, verify-plan po ręcznym wgraniu).
</verification>

<success_criteria>
- Sekcja 39 i 40 zawierają wszystkie 14 funkcji + tabelę + 4 kolumny + 2 zadania pg_cron
- Brak zmian w sekcjach 1–38
</success_criteria>

<output>
After completion, create `.planning/phases/06-rozgrywka-autorytatywna-serwera-i-plynnosc-jak-w-aplikacji/06-02-SUMMARY.md`
</output>
