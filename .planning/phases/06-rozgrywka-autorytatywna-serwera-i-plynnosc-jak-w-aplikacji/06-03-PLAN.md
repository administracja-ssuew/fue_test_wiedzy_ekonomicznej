---
phase: 06-rozgrywka-autorytatywna-serwera-i-plynnosc-jak-w-aplikacji
plan: 03
type: execute
wave: 2
depends_on: ["06-01", "06-02"]
files_modified:
  - scripts/verify-plan.js
  - scripts/verify-prod.js
  - package.json
autonomous: false
requirements: [P6-SC1, P6-SC6]

must_haves:
  truths:
    - "Sekcje 39 i 40 są wgrane na produkcję ytbwmmqwbfcugouourih"
    - "SQL plan_position/sweep_decision dają identyczne wyniki co plan.js na wspólnych fixture'ach"
    - "Zamiatacz pg_cron żyje (ostatni przebieg < 5 s)"
    - "Obecnie wdrożony frontend (stary build 87e7c20) przechodzi sondę po wgraniu 39–40"
  artifacts:
    - path: "scripts/verify-plan.js"
      provides: "Test parzystości JS↔SQL (read-only)"
    - path: "scripts/verify-prod.js"
      provides: "Smoke sekcji 39–40 (istnienie, blokady anona, żywotność zamiatacza)"
    - path: "package.json"
      provides: "skrypt verify-plan"
      contains: "\"verify-plan\""
  key_links:
    - from: "scripts/verify-plan.js"
      to: "src/lib/plan.fixtures.json + rpc plan_position/sweep_decision"
      via: "import fixtures + anon.rpc"
      pattern: "rpc\\(\"plan_position\""
    - from: "scripts/verify-prod.js"
      to: "rpc sweeper_status"
      via: "anon.rpc"
      pattern: "sweeper_status"
---

<objective>
Przygotować automatyczną weryfikację sekcji 39–40, doprowadzić do ich ręcznego wgrania na produkcję (jedyna droga — brak psql/CLI/hasła DB; decyzja CONTEXT „Wgrywanie SQL tylko ręcznie w SQL Editor”), a potem automatycznie potwierdzić: istnienie i blokady (verify-prod), parzystość JS↔SQL (verify-plan) oraz SC6 — stary, wdrożony frontend działa bez zmian.

Purpose: Bramka bezpieczeństwa przed jakąkolwiek zmianą frontu. Testy na produkcji, więc wszystko read-only lub samosprzątające.
Output: `scripts/verify-plan.js`, rozszerzony `scripts/verify-prod.js`, wpis w `package.json`, wgrane sekcje 39–40, zielony raport regresji starego buildu.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-rozgrywka-autorytatywna-serwera-i-plynnosc-jak-w-aplikacji/06-CONTEXT.md
@.planning/phases/06-rozgrywka-autorytatywna-serwera-i-plynnosc-jak-w-aplikacji/06-VALIDATION.md
@.planning/phases/06-rozgrywka-autorytatywna-serwera-i-plynnosc-jak-w-aplikacji/06-02-SUMMARY.md
@scripts/verify-prod.js
@src/lib/plan.js
@src/lib/plan.fixtures.json

<interfaces>
Z scripts/verify-prod.js (wzorzec do powielenia):
```js
const anon = createClient(URL, ANON, { auth: { persistSession: false } });
const DUMMY = "00000000-0000-0000-0000-000000000000";
const ok = (l, d = "") => ...; const bad = (l, d = "") => ...; const note = (l, d = "") => ...;
const isMissing = (e) => e && (e.code === "PGRST202" || /Could not find the function/i.test(e.message || ""));
const isDenied  = (e) => e && (e.code === "42501" || /permission denied/i.test(e.message || ""));
```
Z src/lib/plan.js (06-01): `planPosition(items, anchorMs, pausedAtMs, nowMs)`, `sweepDecision(row, items, nowMs)`, `toMs(ts)`.
Fixture'y: `{ anchorMs, items, position: [{name, t, pausedT, expect:{idx, phase}}], sweep: [{name, t, row:{status, curIdx, qStartedT, revealedIdx, pausedT}, expect:{action, status?, idx?, qStartedT?, revealedIdx?}}] }` — czasy względem `anchorMs`.
RPC (06-02): `plan_position(p_items, p_anchor, p_paused_at, p_at)` → rows `{idx, phase, opens_at, closes_at, reveal_until, question_id, tpq}`; `sweep_decision(p_items, p_anchor, p_paused_at, p_status, p_cur_idx, p_q_started, p_revealed_idx, p_at)` → rows `{action, new_status, new_idx, new_q_started_at, new_revealed_idx}`; `sweeper_status()` → `{cron_installed, job_active, schedule, last_run_age_s, last_status}`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: scripts/verify-plan.js + rozszerzenie verify-prod o sekcje 39–40</name>
  <files>scripts/verify-plan.js, scripts/verify-prod.js, package.json</files>
  <read_first>
    - scripts/verify-prod.js (cały — styl raportu, kody wyjścia, pomocniki)
    - src/lib/plan.js, src/lib/plan.fixtures.json (format fixture'ów)
    - package.json (sekcja scripts)
  </read_first>
  <action>
1. Utwórz `scripts/verify-plan.js` (vite-node, read-only, komentarz nagłówka po polsku jak w verify-prod: co sprawdza, że woła tylko funkcje bez dostępu do tabel). Importy: `import fixtures from "../src/lib/plan.fixtures.json";`, `import { planPosition, sweepDecision, toMs } from "../src/lib/plan.js";`, `createClient` z `@supabase/supabase-js`; klient anon z `VITE_SUPABASE_URL`/`VITE_SUPABASE_ANON_KEY` (brak → exit 1). Pomocniki: `const iso = (ms) => (ms == null ? null : new Date(ms).toISOString());`, `const A = fixtures.anchorMs;`.
   - Każdy `position[k]`: `at = A + t`, `paused = pausedT == null ? null : A + pausedT`; JS `planPosition(fixtures.items, A, paused, at)`; SQL `anon.rpc("plan_position", { p_items: fixtures.items, p_anchor: iso(A), p_paused_at: iso(paused), p_at: iso(at) })` → `data[0]`. Porównaj: `idx`, `phase`, `toMs(opens_at) === js.opensAt`, `toMs(closes_at) === js.closesAt`, `toMs(reveal_until) === js.revealUntil`, `question_id === js.item.id`, `tpq === js.item.tpq`; oba muszą też zgadzać się z `expect.idx`/`expect.phase`.
   - Każdy `sweep[k]`: row JS `{ status, anchorMs: A, pausedAtMs: pausedT == null ? null : A + pausedT, curIdx, qStartedAtMs: qStartedT == null ? null : A + qStartedT, revealedIdx }`; SQL `anon.rpc("sweep_decision", { p_items: fixtures.items, p_anchor: iso(A), p_paused_at: iso(row.pausedAtMs), p_status: status, p_cur_idx: curIdx, p_q_started: iso(row.qStartedAtMs), p_revealed_idx: revealedIdx, p_at: iso(A + t) })` → `data[0]`. Porównaj `action`; gdy ≠ "none": `new_status === js.status`, `new_idx === js.idx`, `toMs(new_q_started_at) === js.qStartedAtMs`, `(new_revealed_idx ?? null) === (js.revealedIdx ?? null)`; plus zgodność z `expect`.
   - Raport `✅/❌ <name>` per przypadek, na końcu `PARZYSTOŚĆ JS↔SQL: <ok>/<all>`; `process.exit(fail ? 1 : 0)`. Błąd PGRST202 → „sekcja 39 nie wgrana na produkcję” i exit 1.
2. `package.json` → w `scripts` dodaj `"verify-plan": "vite-node scripts/verify-plan.js"` (zaraz po `verify-prod`).
3. `scripts/verify-prod.js` — zaktualizuj komentarz nagłówka (sekcje 16–40) i dopisz przed podsumowaniem blok `console.log("\n🧭 SEKCJE 39–40 (plan sesji, zamiatacz, v2):\n")`:
   - istnieją dla anona (nie `isMissing`): `plan_position` (items `[{i:0,id:DUMMY,m:1,tpq:20,lead:10,o:10000,c:30000,r:36000}]`, anchor = at = `new Date().toISOString()`, paused null), `sweep_decision` (te same items, status "waiting"), `get_participant_state` (`{p_code:"PROBE-0000"}` → ok tylko gdy `data?.error === "invalid code"` i `Number(data?.server_now) > 0`), `submit_answer_v2` (DUMMY + `p_code:"PROBE-0000"` → błąd 'invalid code' oznacza, że istnieje), `get_answer_summary_v2` (DUMMY, DUMMY);
   - `anon.from("session_plans").select("session_id").limit(1)` bez błędu → ok („anon czyta plan — bez ans”);
   - `anon.from("quiz_sessions").select("plan_anchor_at,plan_paused_at,revealed_idx,revealed_ans").limit(1)` bez błędu → ok, błąd → bad „kolumny planu BRAK → sekcja 39”;
   - anon ZABLOKOWANY (`isDenied` → ok; `isMissing` → bad „BRAK → sekcja 39”; brak błędu → bad „anon MA DOSTĘP”): `start_quiz_session_v2` ({p_session_id: DUMMY}), `admin_pause_session`, `admin_resume_session`, `admin_skip_question` ({p_session_id: DUMMY, p_idx: 0}), `admin_repeat_question` (jw.), `admin_sweep_session`, `advance_due_sessions` ({});
   - `sweeper_status` ({}): missing → bad „→ sekcja 40”; `!cron_installed` → bad „pg_cron nie zainstalowany → sekcja 40”; `!job_active` → bad „zadanie fue-advance-due nieaktywne”; `last_status === "failed"` → bad; `last_run_age_s == null || last_run_age_s > 5` → bad „zamiatacz nie żyje (ostatni przebieg X s temu)”; inaczej ok z wiekiem i `schedule`.
   Istniejące sprawdzenia sekcji 16–38 (stary kontrakt wdrożonego frontu — V-14) zostają nietknięte.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json');if(p.scripts['verify-plan']!=='vite-node scripts/verify-plan.js')process.exit(1)" && grep -c "sweeper_status\|submit_answer_v2\|get_participant_state\|advance_due_sessions" scripts/verify-prod.js</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` zawiera `"verify-plan": "vite-node scripts/verify-plan.js"`
    - `grep -c "rpc(\"plan_position\"\|rpc(\"sweep_decision\"" scripts/verify-plan.js` ≥ 2
    - `grep -n "plan.fixtures.json" scripts/verify-plan.js` → 1 linia
    - `grep -c "sweeper_status\|submit_answer_v2\|get_participant_state\|advance_due_sessions\|start_quiz_session_v2\|admin_skip_question" scripts/verify-prod.js` ≥ 6
    - `grep -n "get_admin_answer_summary\|advance_session_question\|server_now" scripts/verify-prod.js` — stare sprawdzenia nadal obecne (≥ 3 linie)
    - Przed wgraniem SQL: `npm run verify-plan` kończy się kodem ≠ 0 z komunikatem „sekcja 39 nie wgrana” (to oczekiwane — potwierdza, że skrypt wykrywa brak)
  </acceptance_criteria>
  <done>Skrypty gotowe; wykrywają zarówno brak sekcji, jak i rozjazd JS↔SQL.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Ręczne wgranie sekcji 39 i 40 w SQL Editorze produkcji</name>
  <files>SUPABASE_FIXES.sql</files>
  <read_first>
    - SUPABASE_FIXES.sql (sekcje 39 i 40 — dokładnie ten tekst do wklejenia)
  </read_first>
  <action>
Brak psql/Supabase CLI/hasła DB w `.env` — SQL da się wgrać wyłącznie przez SQL Editor (decyzja CONTEXT). Claude przygotowuje dwa pliki do wklejenia w katalogu scratchpad (NIE w repo): wycina z `SUPABASE_FIXES.sql` blok od `-- ─── 39.` do linii przed `-- ─── 40.` → `sekcja-39.sql`, oraz od `-- ─── 40.` do linii przed stopką `Done.` → `sekcja-40.sql`, i podaje użytkownikowi ich ścieżki.
  </action>
  <what-built>Sekcje 39 (plan sesji, v2 RPC, zamiatacz, łatki kompatybilności) i 40 (pg_cron + sweeper_status) w SUPABASE_FIXES.sql; skrypty verify-prod/verify-plan gotowe do sprawdzenia wyniku.</what-built>
  <how-to-verify>
    1. Otwórz Supabase Dashboard → projekt `ytbwmmqwbfcugouourih` (produkcja z `.env`, NIE `dmoydtavstpurqebkngu`) → SQL Editor.
    2. Uruchom: `select version();` oraz `select extversion from pg_extension where extname='pg_cron';` — zanotuj wynik. Postgres musi być ≥ 15.1.1.61 (wymóg harmonogramu `'1 seconds'`). Jeśli niższy — STOP, zgłoś wersję.
    3. Wklej i uruchom całą zawartość `sekcja-39.sql`. Oczekiwane: „Success. No rows returned” bez błędów.
    4. Wklej i uruchom całą zawartość `sekcja-40.sql`. Oczekiwane: dwa wiersze z `cron.schedule` (numery zadań), bez błędów. Alternatywa przy błędzie uprawnień do rozszerzenia: Dashboard → Integrations → Cron → Enable, potem ponownie `sekcja-40.sql`.
    5. Kontrola: `select jobname, schedule, active from cron.job;` → `fue-advance-due | 1 seconds | t` i `fue-cron-cleanup | */10 * * * * | t`.
    6. Kontrola: `select public.advance_due_sessions();` → liczba (0, jeśli nie trwa żadna sesja z planem).
  </how-to-verify>
  <verify>
    <automated>npm run verify-prod && npm run verify-plan</automated>
  </verify>
  <acceptance_criteria>
    - Użytkownik podał wersję Postgresa (≥ 15.1.1.61) i potwierdził brak błędów w obu sekcjach
    - Po wznowieniu `npm run verify-prod` kończy się kodem 0 („PRODUKCJA GOTOWA pod kątem SQL”), w tym wiersz sweeper_status z `last_run_age_s` ≤ 5
    - `npm run verify-plan` kończy się kodem 0 z `PARZYSTOŚĆ JS↔SQL: N/N`
  </acceptance_criteria>
  <resume-signal>Wpisz „wgrane, Postgres X.Y” albo opisz błąd z SQL Editora</resume-signal>
  <done>Sekcje 39–40 na produkcji; verify-prod i verify-plan zielone.</done>
</task>

<task type="auto">
  <name>Task 3: Regresja SC6 — stary build (87e7c20) na produkcji po wgraniu 39–40</name>
  <files>scripts/verify-prod.js</files>
  <read_first>
    - scripts/probe-gameplay.js (linie 1–80: zmienne PROBE_*, cel, PROBE_CONFIRM; linie 165–190: cleanup)
    - .planning/phases/06-rozgrywka-autorytatywna-serwera-i-plynnosc-jak-w-aplikacji/06-VALIDATION.md (V-14, V-15)
  </read_first>
  <action>
Cel: dowód, że wdrożony front działa bez zmian (V-15). Kroki (Git Bash; ścieżki Windows — worktree w scratchpadzie sesji, nie w `/tmp`):
1. `git worktree add "<scratchpad>/fue-old" 87e7c20` (kod `src/` = wdrożony `c2180d6`); `cp .env "<scratchpad>/fue-old/.env"`; w worktree `npm ci` i `npm run build`.
2. W tle z worktree: `npx vite preview --port 4174 --strictPort` (run_in_background).
3. Z GŁÓWNEGO repo (sonda jest tu jeszcze niezmieniona — plan 06-08 zmienia ją później): `PROBE_TARGET=prod PROBE_CONFIRM=1 PROBE_APP_URL=http://localhost:4174 npm run sonda` (PowerShell: `$env:PROBE_TARGET="prod"; $env:PROBE_CONFIRM="1"; $env:PROBE_APP_URL="http://localhost:4174"; npm run sonda`). Stary panel startuje sesję przez stary `start_quiz_session` (łatka 39.9 czyści kolumny planu) → sesja BEZ planu → zamiatacz jej nie dotyka, stary kierowca admina działa, `advance_session_question` przechodzi (`plan_anchor_at IS NULL`).
4. Po sondzie: zatrzymaj preview, `git worktree remove --force "<scratchpad>/fue-old"`.
5. Sprawdź sprzątanie: `node -e` z klientem service key (`SUPABASE_SERVICE_KEY` z `.env`) — `session_plans` nie zawiera wierszy dla sesji miasta sondy utworzonych w tym przebiegu (stary panel ich nie tworzy; oczekiwane 0).
Zapisz w SUMMARY: wynik sondy (liczba FAIL = 0), wersję Postgresa z Task 2, wynik verify-prod/verify-plan. `scripts/verify-prod.js` modyfikuj tylko, jeśli sonda ujawni brak sprawdzenia starego kontraktu (dopisz je wtedy).
  </action>
  <verify>
    <automated>npm run verify-prod && npm run verify-plan</automated>
  </verify>
  <acceptance_criteria>
    - Sonda na starym buildzie kończy się kodem 0 (brak „FAIL” w raporcie)
    - `git worktree list` nie zawiera już `fue-old`
    - `npm run verify-prod` i `npm run verify-plan` kończą się kodem 0
    - SUMMARY zawiera wiersz „SC6 regresja starego buildu: OK” z datą i liczbą pytań sondy
  </acceptance_criteria>
  <done>SC6 potwierdzone dla stanu „SQL wgrany, stary front”.</done>
</task>

</tasks>

<verification>
`npm run verify-prod` (0), `npm run verify-plan` (0), sonda na starym buildzie (0). Produkcja nie ma resztek sondy.
</verification>

<success_criteria>
- Sekcje 39–40 na produkcji, zamiatacz żyje
- Parzystość JS↔SQL potwierdzona na wszystkich fixture'ach
- Wdrożony front działa bez zmian (SC6)
</success_criteria>

<output>
After completion, create `.planning/phases/06-rozgrywka-autorytatywna-serwera-i-plynnosc-jak-w-aplikacji/06-03-SUMMARY.md`
</output>
