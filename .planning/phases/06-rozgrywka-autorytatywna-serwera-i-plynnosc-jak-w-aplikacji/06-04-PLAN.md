---
phase: 06-rozgrywka-autorytatywna-serwera-i-plynnosc-jak-w-aplikacji
plan: 04
type: execute
wave: 2
depends_on: ["06-01", "06-02"]
files_modified:
  - src/lib/supabase.js
  - src/lib/participantState.js
  - src/lib/participantState.test.js
  - src/hooks/useParticipantGame.js
autonomous: true
requirements: [P6-SC2, P6-SC3, P6-SC5]

must_haves:
  truths:
    - "Jedyną ścieżką restore uczestnika jest snapshot get_participant_state (start, refresh, visibilitychange→visible, online, reconnect socketu)"
    - "Stan uczestnika i plan są w localStorage z kluczem ważności session_id; stary klucz z sessionStorage jest jednorazowo migrowany"
    - "Po refreshu faza liczy się od pierwszej klatki z zapamiętanego planu, a offset zegara dostaje próbkę z server_now snapshotu"
    - "Wybór odpowiedzi jest blokowany optymistycznie i ponawiany do closes_at + 1,5 s; pusty zapis po deadline idzie z jitterem 0–1000 ms"
    - "Kolor poprawnej odpowiedzi pochodzi wyłącznie z revealed_* / reveal snapshotu — nigdy z odpowiedzi submit"
    - "Tryb DEMO (bez kluczy Supabase) działa na tym samym planie budowanym lokalnie"
    - "Przypięte id zakończonej sesji nie zatrzymuje uczestnika: snapshot przełącza na nowszą sesję miasta, hook resetuje plan/odpowiedzi, a Lobby przekazuje id nowej sesji do refresh(sessionIdHint)"
    - "Każdy snapshot wyzwolony przez Realtime (broadcast, UPDATE statusu, kotwica bez planu, reconnect, brak revealAns) ma jitter ≥ 0–1000 ms; bez jitteru tylko montaż, refresh(), visible i online"
    - "Kotwica bez planu daje fazę plan_loading, nie legacy"
  artifacts:
    - path: "src/lib/supabase.js"
      provides: "Wrappery v2 + implementacje DEMO"
      exports: ["startQuizSessionV2", "submitAnswerV2", "getParticipantState", "getSessionPlan", "getAnswerSummaryV2", "adminPauseSession", "adminResumeSession", "adminSkipQuestion", "adminRepeatQuestion", "getSweeperStatus", "adminSweepSession"]
    - path: "src/lib/participantState.js"
      provides: "Czyste funkcje: localStorage, normalizacja snapshotu, scalanie zdarzeń, reveal"
    - path: "src/hooks/useParticipantGame.js"
      provides: "Snapshot + Realtime jako sygnał + ticker rAF → widok uczestnika"
  key_links:
    - from: "src/hooks/useParticipantGame.js"
      to: "getParticipantState"
      via: "snapshot przy starcie/visible/online/reconnect"
      pattern: "getParticipantState\\("
    - from: "src/hooks/useParticipantGame.js"
      to: "projectPlanState"
      via: "ticker requestAnimationFrame + serverNow()"
      pattern: "requestAnimationFrame"
    - from: "src/hooks/useParticipantGame.js"
      to: "addClockSample"
      via: "t0/t1 wokół snapshotu + server_now"
      pattern: "addClockSample\\("
---

<objective>
Warstwa danych i stan gry uczestnika w nowym modelu: wrappery RPC v2 (z DEMO), czyste funkcje stanu (localStorage, snapshot, scalanie zdarzeń) z testami, oraz hook `useParticipantGame` — jedyne źródło fazy dla ekranu uczestnika.

Purpose: Decyzje CONTEXT „Odświeżenie / reconnect” (jeden RPC-snapshot, localStorage z session_id) i „Punktacja i uczciwość” (correct_ans dopiero w reveal). Serce SC2/SC3.
Output: rozszerzony `src/lib/supabase.js`, nowe `src/lib/participantState.js` (+test), `src/hooks/useParticipantGame.js`. App.jsx NIE jest tu zmieniany (06-05).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-rozgrywka-autorytatywna-serwera-i-plynnosc-jak-w-aplikacji/06-CONTEXT.md
@.planning/phases/06-rozgrywka-autorytatywna-serwera-i-plynnosc-jak-w-aplikacji/06-RESEARCH.md
@src/lib/plan.js
@src/lib/serverClock.js

<interfaces>
Z src/lib/supabase.js (istniejące):
```js
export const DEMO; export const supabase;
export async function getModules();                       // [{ id, name, icon, color, timePerQ, desc }]
export async function getQuestions(city);                 // DEMO: localStorage `fue_questions_${city}`
export async function getLiveAnswerSummary(sessionId, questionId); // { total, correct, ans }
export async function saveAnswer({ sessionId, participantCode, participantName, city, questionId, module, chosen, isCorrect, points, responseTimeS });
// DEMO sesje: localStorage `fue_session_${city}` i `fue_session_${city}_practice`; odpowiedzi: `fue_answers`
// Wzorzec błędu braku funkcji: error.code === "PGRST202" || /Could not find the function/i.test(error.message || "")
```
Z src/lib/plan.js (06-01): `toMs, buildPlanItems, planPosition, projectPlanState, isRevealed, resumeAnchor, skipAnchor, repeatAnchor, answerResponseMs, REVEAL_GATE_MS`.
Z src/lib/serverClock.js (06-01): `serverNow(), addClockSample({t0,t1,serverMs})`.

Snapshot `get_participant_state` (06-02) — znaczniki czasu jako epoch ms:
```json
{ "server_now": 0, "error": null | "invalid code",
  "session": { "id", "city", "status", "is_practice", "bg", "bg_mobile", "name", "plan_anchor_at": ms|null, "plan_paused_at": ms|null } | null,
  "position": { "idx", "phase", "opens_at", "closes_at", "reveal_until" } | null,
  "plan": [ { "i", "id", "m", "tpq", "lead", "o", "c", "r", "q", "opts" } ] | null,
  "my_answers": [ { "question_id", "chosen", "is_correct": bool|null } ],
  "reveal": { "idx", "ans" } | null,
  "correct_total": 0 }
```
`submit_answer_v2` → `{ accepted, duplicate, chosen }` lub błąd (`'session paused'`, `'time is up'`, `'question not started'`, `'invalid code'`, …).
Akcje admina v2 → `{ ok, reason, session: <wiersz quiz_sessions, ISO timestamps> }`.
`postgres_changes` UPDATE quiz_sessions niesie `new` z polami `status, plan_anchor_at, plan_paused_at, revealed_idx, revealed_ans, current_question_idx, q_started_at` (ISO).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: supabase.js — wrappery v2 + DEMO na planie lokalnym</name>
  <files>src/lib/supabase.js</files>
  <read_first>
    - src/lib/supabase.js (linie 1–60 klient/DEMO; 360–470 updateSession/advanceSessionQuestion/startQuizSession/getSessionForCity — wzorce DEMO po kluczach `fue_session_${city}${suffix}`; 519–575 saveAnswer/submitAnswer/getParticipantAnswers; 676–705 getLiveAnswerSummary; 774–803 fetchModules/getModules)
    - src/lib/plan.js
  </read_first>
  <action>
Dopisz (nie usuwaj istniejących eksportów — stare są potrzebne do 06-11) sekcję `// ── Faza 6: rozgrywka z planu (RPC v2) ──` z pomocnikiem `const isMissingFn = (e) => e && (e.code === "PGRST202" || /Could not find the function/i.test(e.message || ""));` i DEMO-pomocnikiem `demoFindSession(sessionId)` → `{ key, s } | null` (pętla po 5 miastach × `["", "_practice"]`, jak w `updateSession`). Import z `./plan.js`: `buildPlanItems, planPosition, isRevealed, resumeAnchor, skipAnchor, repeatAnchor, answerResponseMs, toMs`.

Eksporty (wszystkie zwracają obiekty, bez throw — konwencja `{ data, error }` repo):
- `startQuizSessionV2(sessionId)` → `{ ok, reason, session, error }`. Prod: `await supabase.auth.getSession()` potem `rpc("start_quiz_session_v2", { p_session_id })`; `isMissingFn` → `{ ok:false, error: "Na bazie brak sekcji 39 (start_quiz_session_v2) — wgraj SQL." }`. DEMO: pytania `JSON.parse(localStorage.getItem(\`fue_questions_${s.city}\`)||"[]")` posortowane po (module, sort_order ?? 0, id), `buildPlanItems(qs, await getModules())`, zapis `localStorage["fue_plan_" + id] = JSON.stringify(items)`, sesja `{...s, status:"running", current_question_idx:0, plan_anchor_at: new Date().toISOString(), plan_paused_at:null, revealed_idx:null, revealed_ans:null, q_started_at: ISO(anchor + items[0].o)}`; brak pytań → `{ ok:false, reason:"no questions" }`.
- `getSessionPlan(sessionId)` → `items | null`. Prod: `supabase.from("session_plans").select("items").eq("session_id", sessionId).maybeSingle()`. DEMO: `fue_plan_${id}`.
- `getParticipantState(code, { sessionId = null, includePlan = true } = {})` → `{ data, error, t0, t1 }` (`t0/t1 = Date.now()` wokół wywołania). Prod: `rpc("get_participant_state", { p_code: code, p_session_id: sessionId, p_include_plan: includePlan })`. DEMO: zbuduj ten sam kształt JSON lokalnie: kod z `fue_codes`/obecnego mechanizmu DEMO walidacji (użyj tej samej funkcji, której używa `validateParticipantCode` w DEMO), sesja wg priorytetu `getSessionForCity` z tą samą regułą porzucenia nieaktualnej przypiętej sesji co 39.6b (przypięta w `results`/`ended` + istnieje nowsza nie-ended sesja miasta → ta nowsza), `plan` = items + `q`/`opts` z `fue_questions_${city}` (bez `ans`), pozycja z `planPosition`, `my_answers` z `fue_answers` (is_correct tylko gdy `isRevealed` lub status results/ended), `reveal` wg reguły 39.6b, `server_now = Date.now()`.
- `submitAnswerV2({ sessionId, participantCode, participantName, questionId, chosen })` → `{ accepted, duplicate, chosen, error, retryable }`. Prod: `rpc("submit_answer_v2", {...})`; `retryable = true` gdy brak `error.code` i komunikat pasuje do `/fetch|network|timeout|Failed/i`, albo komunikat zawiera `session paused`; inaczej false. DEMO: walidacja okna z planu (`answerResponseMs`, `closes + REVEAL_GATE_MS`), `saveAnswer` z `isCorrect` z `ans` pytania DEMO (dedupe po sessionId+code+questionId), zwraca `{ accepted:true, duplicate, chosen }`.
- `getAnswerSummaryV2(sessionId, questionId)` → `{ total, correct, ans }`; `isMissingFn` → fallback `getLiveAnswerSummary`. DEMO: liczniki z `fue_answers`, `correct`/`ans` tylko gdy `isRevealed`.
- `adminPauseSession(id)`, `adminResumeSession(id)`, `adminSkipQuestion(id, idx)`, `adminRepeatQuestion(id, idx)` → `{ ok, reason, session, error }`; prod: `await supabase.auth.getSession()` + `rpc("admin_pause_session"|"admin_resume_session"|"admin_skip_question"|"admin_repeat_question", { p_session_id, p_idx? })`. DEMO: te same przesunięcia kotwicy przez `resumeAnchor/skipAnchor/repeatAnchor` na obiekcie sesji w localStorage (pauza: `status:"paused", plan_paused_at: ISO(now)`; wznowienie: nowa kotwica, `plan_paused_at:null`, `status:"running"`; `q_started_at`/`current_question_idx` z `planPosition`); null z skip/repeat → `{ ok:false, reason:"noop" }`.
- `getSweeperStatus()` → json | null (prod `rpc("sweeper_status")`, błąd → null; DEMO `{ cron_installed:true, job_active:true, last_run_age_s:0, last_status:"succeeded" }`).
- `adminSweepSession(id)` → `{ error }` (prod `rpc("admin_sweep_session", { p_session_id })`; DEMO: jeśli `planPosition(...).phase === "finished"` ustaw `status:"results"`).
  </action>
  <verify>
    <automated>npx vitest run && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^export async function \(startQuizSessionV2\|submitAnswerV2\|getParticipantState\|getSessionPlan\|getAnswerSummaryV2\|adminPauseSession\|adminResumeSession\|adminSkipQuestion\|adminRepeatQuestion\|adminSweepSession\)\|^export async function getSweeperStatus" src/lib/supabase.js` → 11
    - `grep -c "rpc(\"submit_answer_v2\"\|rpc(\"get_participant_state\"\|rpc(\"start_quiz_session_v2\"\|rpc(\"get_answer_summary_v2\"" src/lib/supabase.js` → 4
    - Stare eksporty nadal obecne: `grep -c "^export async function \(submitAnswer\|advanceSessionQuestion\|startQuizSession\|getParticipantAnswers\)(" src/lib/supabase.js` → 4
    - `submitAnswerV2` nie czyta `is_correct`/`correct_ans` z odpowiedzi: `awk '/export async function submitAnswerV2/,/^}/' src/lib/supabase.js | grep -c "correct_ans"` → 0
    - `npm run build` kończy się kodem 0; `npx vitest run` zielone
  </acceptance_criteria>
  <done>Kompletne API v2 dla uczestnika, LiveView i panelu; DEMO działa na planie lokalnym.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: participantState.js — localStorage, normalizacja snapshotu, scalanie zdarzeń (TDD)</name>
  <files>src/lib/participantState.js, src/lib/participantState.test.js</files>
  <read_first>
    - src/App.jsx (linie 126–141 restore z sessionStorage, 529–545 handleCodeSuccess — obecne klucze `fue_participant`, `fue_bg`)
    - src/lib/plan.js (toMs)
    - src/test-setup.js (jsdom — localStorage/sessionStorage dostępne)
  </read_first>
  <behavior>
    - loadParticipant(): gdy localStorage pusty, a sessionStorage ma `fue_participant` → przenosi do localStorage, usuwa z sessionStorage, zwraca obiekt; zepsuty JSON → null i usunięcie klucza
    - saveGameCache(sessionId, {plan, session}) + loadGameCache(sessionId) → zwraca cache tylko dla tego samego sessionId; inny sessionId → null
    - normalizeSnapshot(json) → { session (ms), plan, myAnswers: { [qid]: { chosen, status: "saved", correct } }, reveal, correctTotal, serverNowMs, error }
    - mergeSessionRow(session, row) → pola status/plan_anchor_at/plan_paused_at (toMs)/revealed_idx/revealed_ans nadpisane z row; brak pola w row → zostaje stare
    - revealAnsFor(session, reveal, idx): session.revealed_idx === idx → session.revealed_ans; reveal?.idx === idx → reveal.ans; inaczej null
    - planChanged(prevSession, nextSession): true gdy |anchor różnica| ≥ 2 ms lub zmiana plan_paused_at/status
    - snapshotSessionId(hint, currentSession): hint ?? currentSession?.id ?? null
    - applySnapshot(prev, normalized) — ta sama sesja: plan zachowany, gdy snapshot przyszedł bez planu; lokalne wpisy "pending" mają pierwszeństwo przed brakiem wpisu w snapshocie
    - applySnapshot — „nieaktualne sessionId”: prev = sesja A (status ended, plan, myAnswers z 2 wpisami), snapshot = sesja B (waiting, bez planu) → wynik: session B, plan null, myAnswers {}, reveal null, correctTotal 0, `switched: true` (nic z sesji A nie przecieka)
  </behavior>
  <action>
Utwórz `src/lib/participantState.js` (czyste funkcje, bez Supabase):
```js
export const PARTICIPANT_KEY = "fue_participant";   // { code, name, surname, city, sessionId? }
export const GAME_CACHE_KEY  = "fue_game_cache";    // { sessionId, plan, session, savedAt }
export const BG_KEY          = "fue_bg";
export function loadParticipant()        // localStorage → obiekt | null; jednorazowa migracja z sessionStorage (+ fue_bg)
export function saveParticipant(p)
export function clearParticipant()       // usuwa PARTICIPANT_KEY, GAME_CACHE_KEY, BG_KEY z localStorage i sessionStorage
export function saveGameCache(sessionId, { plan, session })
export function loadGameCache(sessionId) // null gdy brak / inny sessionId / zepsuty JSON
export function normalizeSnapshot(json)  // jw. w <behavior>; session.plan_anchor_at/plan_paused_at już w ms (przepuść przez toMs dla bezpieczeństwa)
export function mergeSessionRow(session, row)
export function revealAnsFor(session, reveal, idx)
export function planChanged(prev, next)
export function snapshotSessionId(hint, currentSession)   // id wysyłane jako p_session_id
export function applySnapshot(prev, normalized)
  // prev = { session, plan, myAnswers, reveal, correctTotal }; zwraca nowy taki obiekt + switched: boolean
  // switched = normalized.session?.id !== prev.session?.id → stan wyłącznie ze snapshotu (plan = normalized.plan ?? null)
```
Testy w `src/lib/participantState.test.js` zgodnie z <behavior> (w tym osobny `it` z nazwą zawierającą „nieaktualne sessionId”) (czyść `localStorage`/`sessionStorage` w `beforeEach`). RED → GREEN.
  </action>
  <verify>
    <automated>npx vitest run src/lib/participantState.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/lib/participantState.test.js` zielone, ≥ 11 testów; `npx vitest run src/lib/participantState.test.js -t "nieaktualne sessionId"` uruchamia ≥ 1 test i przechodzi
    - `grep -n "sessionStorage" src/lib/participantState.js` występuje tylko w migracji (`loadParticipant`) i `clearParticipant`
    - `grep -c "^export function" src/lib/participantState.js` ≥ 11
  </acceptance_criteria>
  <done>Stan lokalny uczestnika w localStorage, testowalny bez sieci.</done>
</task>

<task type="auto">
  <name>Task 3: useParticipantGame — snapshot + sygnał Realtime + ticker rAF + optymistyczny zapis</name>
  <files>src/hooks/useParticipantGame.js</files>
  <read_first>
    - src/App.jsx (linie 222–381: kanał `quiz-${id}`, dozorca 3 s, `keepRealtimeAlive`, komentarze o umierającym sockecie — skopiuj wzorzec dozorcy; 388–423 zapis odpowiedzi)
    - src/lib/participantState.js, src/lib/plan.js, src/lib/serverClock.js
    - src/lib/supabase.js (nowe wrappery z Task 1)
  </read_first>
  <action>
Utwórz `src/hooks/useParticipantGame.js` — `export default function useParticipantGame(participant)` (participant `{ code, name, surname, city } | null`). Zwraca:
```js
{ loadState,      // "idle" | "loading" | "ready" | "invalid_code" | "error"
  session,        // { id, city, status, is_practice, bg, bg_mobile, plan_anchor_at(ms), plan_paused_at(ms), revealed_idx, revealed_ans } | null
  plan,           // items z q/opts | null
  view,           // projectPlanState(...) | { phase: "no_session" } ; fazy: lobby|intro|countdown|quiz|reveal|paused|finished|results|ended|legacy|plan_loading|no_session
  myAnswers,      // { [questionId]: { chosen, status: "pending"|"saved"|"failed", correct: bool|null } }
  correctTotal, revealAns, pick,
  refresh }       // refresh(sessionIdHint?) — hint wysyłany jako p_session_id zamiast przypiętego id
```
Mechanika (komentarze po polsku, dlaczego):
1. Inicjalizacja SYNCHRONICZNA z cache: `loadGameCache(participant.sessionId)` → jeśli jest, `session`/`plan` od razu, `loadState = "loading"`, widok liczony od pierwszej klatki (SC2 — brak pustego ekranu).
2. `snapshot({ includePlan, sessionIdHint })`: `includePlan` domyślnie `!planRef.current` (wymuś `true`, gdy `sessionIdHint` ≠ bieżące `session.id`); `const { data, error, t0, t1 } = await getParticipantState(code, { sessionId: snapshotSessionId(sessionIdHint, sessionRef.current), includePlan })`; `addClockSample({ t0, t1, serverMs: Number(data.server_now) })`; `data.error === "invalid code"` → `loadState = "invalid_code"`; inaczej `const next = applySnapshot(prev, normalizeSnapshot(data))` → ustaw session/plan/myAnswers/correctTotal/reveal z `next`. Gdy `next.switched` (serwer zwrócił inną sesję niż przypięta — np. przypięta próba jest `ended`, a admin utworzył nową sesję) → wyczyść refy per-sesja (pusty zapis, „raz na idx” dla revealAns, znacznik `markCodeUsed` zostaje w App), a jeśli nowa sesja ma `plan_anchor_at` i `plan` nie przyszedł → natychmiast drugi snapshot z planem. `saveGameCache(session.id, { plan, session })`; `saveParticipant({...participant, sessionId: session?.id ?? null})` (id przypinane zawsze na to, co zwrócił serwer — serwer sam porzuca nieaktualne przypięcie, reguła 39.6b; NIE czyść id przy `ended`, bo zniknąłby ekran wyników); `loadState = "ready"`. Równoległe wywołania scalaj (flaga in-flight + jedno oczekujące; oczekujące zachowuje najnowszy `sessionIdHint`).
3. Wyzwalacze snapshotu. Pomocnik `snapshotJittered(opts, maxMs)` = `setTimeout(() => snapshot(opts), Math.random() * maxMs)` (z czyszczeniem w cleanup). BEZ jitteru (akcja jednego klienta, nie masowa): montaż; `refresh()`; `visibilitychange` → visible; `window` `online` (Pułapka 11). Z jitterem (sygnał Realtime dociera do ~500 telefonów naraz — bez rozrzutu 500 jednoczesnych RPC, na starcie z ~20 KB planu): ponowny `SUBSCRIBED` kanału po pierwszym → `snapshotJittered(..., 2000)`; broadcast `quiz_event` → `snapshotJittered({ includePlan: false }, 1000)`; zmiana statusu na `results`/`ended` z UPDATE → `snapshotJittered({ includePlan: false }, 1000)` (finalne is_correct); kotwica bez planu (`plan_loading`) → `snapshotJittered({ includePlan: true }, 1000)`; brak `revealAns` przy `phase==="reveal"` gdy `serverNow() > closesAt + 3000` → `snapshotJittered({ includePlan: false }, 1000)` (raz na idx — Pułapka 2); siatka bezpieczeństwa co 15 s (`includePlan:false`, interwał startowany z losowym przesunięciem 0–15 s).
4. Kanał `supabase.channel(\`quiz-${session.id}\`)` (gdy nie DEMO i jest `session.id`): `.on("broadcast", { event: "quiz_event" }, …)` jako SYGNAŁ; `.on("postgres_changes", { event: "UPDATE", schema: "public", table: "quiz_sessions", filter: \`id=eq.${id}\` }, ({ new: row }) => …)` → `mergeSessionRow`; gdy `row.plan_anchor_at` jest, a `plan` brak → faza `plan_loading` (projekcja) + `snapshotJittered({ includePlan: true }, 1000)`. Przed `channel()` `try { supabase.realtime.connect(); } catch (_) {}`. Dozorca co 3 s jak w App.jsx 361–368 (stan ≠ joined/joining → removeChannel + connect + snapshot). DEMO: zamiast kanału poll snapshotu co 1 s.
5. Ticker: pętla `requestAnimationFrame` licząca `projectPlanState({ items: plan, anchorMs: session.plan_anchor_at, pausedAtMs: session.plan_paused_at, status: session.status, nowMs: serverNow() })`; `setView` TYLKO gdy zmieni się klucz `${phase}|${idx}|${secondsLeft}|${opensAt}` (nie co klatkę). Brak sesji → `{ phase: "no_session" }`. Ticker działa także offline (projekcja lokalna — SC3).
6. `pick(choice)`: tylko gdy `view.phase === "quiz"` i brak wpisu dla `view.item.id`; natychmiast `myAnswers[qid] = { chosen, status: "pending", correct: null }` (optimistic lock-in); pętla zapisu: `submitAnswerV2`; `accepted` → `status: "saved"`, `chosen` z odpowiedzi serwera (duplikat → zapisany wybór); `retryable` → ponów co 700 ms dopóki `serverNow() <= closesAt + REVEAL_GATE_MS` (dla „session paused” czekaj, aż `view.phase` znów będzie „quiz”, i ponów); po oknie lub błąd nie-retryable → `status: "failed"`.
7. Pusty zapis: gdy `view.phase` przechodzi na `"reveal"` dla idx, a brak wpisu dla `item.id` → `setTimeout(() => submitAnswerV2({... chosen: null }), Math.random() * 1000)` — raz na item (ref), bez zmiany UI (Pułapka 7).
8. `revealAns = revealAnsFor(session, reveal, view.idx)`; gdy ≠ null dla item, uzupełnij `myAnswers[item.id].correct = chosen === revealAns` i przelicz `correctTotal` (snapshot + lokalnie odsłonięte, bez podwójnego liczenia — licz jako liczbę wpisów z `correct === true`).
9. `refresh = (sessionIdHint) => snapshot({ includePlan: !planRef.current, sessionIdHint })` — Lobby woła przy starcie quizu z id wykrytej sesji (`onStartQuiz(s)` → `refresh(s.id)`, 06-05), więc nawet przypięte stare id nie zatrzyma uczestnika.
Hook NIE importuje `useModules` i NIE czyta `timePerQ` (SC4). Cleanup: cancelAnimationFrame, removeChannel, clearInterval/clearTimeout, usunięcie listenerów.
  </action>
  <verify>
    <automated>npx vitest run && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "requestAnimationFrame" src/hooks/useParticipantGame.js` ≥ 1; `grep -c "addClockSample(" src/hooks/useParticipantGame.js` ≥ 1
    - `grep -c "\"online\"\|visibilitychange" src/hooks/useParticipantGame.js` ≥ 2
    - `grep -n "timePerQ\|useModules\|advanceSessionQuestion\|submitAnswer(" src/hooks/useParticipantGame.js` → 0 linii
    - `grep -n "Math.random() \* 1000" src/hooks/useParticipantGame.js` → ≥ 1 linia (pusty zapis z jitterem)
    - `grep -c "snapshotJittered(" src/hooks/useParticipantGame.js` ≥ 5 (definicja + reconnect, broadcast, status końcowy, kotwica bez planu, brak revealAns)
    - `grep -c "applySnapshot(\|snapshotSessionId(" src/hooks/useParticipantGame.js` ≥ 2; `grep -n "sessionIdHint" src/hooks/useParticipantGame.js` → ≥ 2 linie
    - `grep -n "sessionStorage" src/hooks/useParticipantGame.js` → 0 linii
    - `npm run build` kod 0; `npx vitest run` zielone
  </acceptance_criteria>
  <done>Hook gotowy do podpięcia w App.jsx (06-05); cała logika gry uczestnika w jednym miejscu.</done>
</task>

</tasks>

<verification>
`npx vitest run` + `npm run build` zielone. Integracja end-to-end — sonda w 06-08.
</verification>

<success_criteria>
- 11 wrapperów v2 z DEMO; stare eksporty nienaruszone
- participantState z testami; hook bez timePerQ/sessionStorage
</success_criteria>

<output>
After completion, create `.planning/phases/06-rozgrywka-autorytatywna-serwera-i-plynnosc-jak-w-aplikacji/06-04-SUMMARY.md`
</output>
