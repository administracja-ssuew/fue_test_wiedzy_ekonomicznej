---
phase: 06-rozgrywka-autorytatywna-serwera-i-plynnosc-jak-w-aplikacji
plan: 05
type: execute
wave: 3
depends_on: ["06-04"]
files_modified:
  - src/screens/Quiz.jsx
  - src/screens/Break.jsx
  - src/screens/WaitingResults.jsx
  - src/App.jsx
autonomous: true
requirements: [P6-SC1, P6-SC2, P6-SC3, P6-SC4]

must_haves:
  truths:
    - "Ekran uczestnika = switch(view.phase) z useParticipantGame; brak imperatywnej maszyny stanów i timerów setInterval w App.jsx"
    - "Czas pytania na ekranie uczestnika pochodzi z item.tpq planu, nie z ModulesContext"
    - "Uczestnik nigdy nie zapisuje przejścia pytania (brak advanceSessionQuestion/armAdvanceFallback w App.jsx)"
    - "Po refreshu stan uczestnika wraca z localStorage bez ponownego wpisywania kodu"
    - "document.body ma data-fue-phase/q/remaining/locked do odczytu przez sondę"
    - "Kolor poprawnej odpowiedzi w reveal pojawia się dopiero, gdy przyjdzie revealAns; wcześniej „Sprawdzamy…”"
  artifacts:
    - path: "src/App.jsx"
      provides: "Routing uczestnika z projekcji planu"
      contains: "useParticipantGame"
    - path: "src/screens/Quiz.jsx"
      provides: "UI pytania na propsach z planu (tpq, secondsLeft, phase, revealAns)"
    - path: "src/screens/Break.jsx"
      provides: "Ekran pauzy — czysto prezentacyjny"
    - path: "src/screens/WaitingResults.jsx"
      provides: "Ekran końca pytań — czysto prezentacyjny"
  key_links:
    - from: "src/App.jsx"
      to: "src/hooks/useParticipantGame.js"
      via: "const game = useParticipantGame(participant)"
      pattern: "useParticipantGame\\(participant\\)"
    - from: "src/App.jsx"
      to: "document.body.dataset"
      via: "useEffect ustawiający fuePhase/fueQ/fueRemaining/fueLocked"
      pattern: "dataset\\.fuePhase"
---

<objective>
Przepiąć ścieżkę uczestnika w `App.jsx` na `useParticipantGame`: routing = funkcja fazy z planu, restore z localStorage, atrybuty `data-fue-*` dla sondy. Ekrany `Quiz`, `Break`, `WaitingResults` stają się prezentacyjne.

Purpose: Decyzje CONTEXT „Model czasu” (faza = czysta funkcja serverNow/plan/pauzy, tpq wyłącznie z planu) i „Przejścia” (awaryjny kierowca uczestników do usunięcia — w nowym bundlu go nie ma). SC1–SC4 po stronie uczestnika.
Output: przebudowane `src/App.jsx`, `src/screens/Quiz.jsx`, `src/screens/Break.jsx`, `src/screens/WaitingResults.jsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-rozgrywka-autorytatywna-serwera-i-plynnosc-jak-w-aplikacji/06-CONTEXT.md
@.planning/phases/06-rozgrywka-autorytatywna-serwera-i-plynnosc-jak-w-aplikacji/06-04-SUMMARY.md
@src/App.jsx
@src/screens/Quiz.jsx

<interfaces>
Z src/hooks/useParticipantGame.js (06-04):
```js
const { loadState, session, plan, view, myAnswers, correctTotal, revealAns, pick, refresh } = useParticipantGame(participant);
// view = { phase, underPhase, idx, item: { i, id, m, tpq, lead, o, c, r, q, opts }, opensAt, closesAt, revealUntil, remainingMs, secondsLeft, firstOfModule }
// view.phase ∈ lobby|intro|countdown|quiz|reveal|paused|finished|results|ended|legacy|no_session
// myAnswers[qid] = { chosen, status: "pending"|"saved"|"failed", correct }
```
Z src/lib/participantState.js (06-04): `loadParticipant, saveParticipant, clearParticipant, BG_KEY`.
Z src/lib/supabase.js: `markCodeUsed(code, sessionId)`, `getCityBg(city)`, `logoutAdmin`, `keepRealtimeAlive`.
Istniejące ekrany: `ModuleIntroFS({ mod, secondsLeft })`, `Countdown({ num })`, `Lobby({ participant, isDesktop, isPractice, onStartQuiz, onPractice })`, `Ended({ participant, myPts, allAnswers, isPractice, onGoHome })`, `CodeEntry({ onBack, onSuccess })`.
ModulesContext: `useModules()` → `[{ id, name, icon, color, timePerQ, desc }]` — w nowym kodzie uczestnika używać TYLKO name/icon/color.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Quiz/Break/WaitingResults — nowe propsy z planu, ekrany prezentacyjne</name>
  <files>src/screens/Quiz.jsx, src/screens/Break.jsx, src/screens/WaitingResults.jsx</files>
  <read_first>
    - src/screens/Quiz.jsx (cały — zachować wygląd, useAntiCheat, pasek potwierdzenia, overlay ostrzeżenia)
    - src/screens/Break.jsx, src/screens/WaitingResults.jsx (całe — zachować teksty „Przerwa/Wstrzymano”, „Koniec testu/Wszystkie pytania zosta…”, bo sonda je czyta jako fallback)
  </read_first>
  <action>
`Quiz.jsx` — nowa sygnatura:
```js
export default function Quiz({ item, mod, phase, secondsLeft, opensAt, closesAt, revealUntil, picked, answerStatus, correctAns,
  qNumGlobal, totalQuestions, qNumInModule, moduleCount, correctTotal, isDesktop, isPractice, participantCode, sessionId, onPick })
```
- `item.q`, `item.opts` zamiast `currentQ.q/opts`; `mod` wyłącznie do `name/icon/color` (usuń `useModules` z Quiz, jeśli zbędny).
- `answered = phase === "reveal"`; `timerPct = phase === "quiz" ? secondsLeft / item.tpq : 0`; kolory licznika progami `item.tpq * 0.5` / `* 0.25` (NIGDY `mod.timePerQ`).
- Nagłówek bez zmian tekstowo: `Pytanie {qNumInModule} / {moduleCount} · #{qNumGlobal}/{totalQuestions}` (sonda parsuje ten format).
- Usuń lokalny `resultSec` z `setInterval` — licznik „następne” w pasku wyniku = `secondsLeft` (w fazie reveal hook liczy do `revealUntil`).
- Reveal: kolorowanie poprawna/błędna TYLKO gdy `correctAns != null`; gdy `answered && correctAns == null` → pasek dolny z tekstem „Sprawdzamy odpowiedź…” (Pułapka 2); usuń fallback `currentQ.ans`.
- Pasek potwierdzenia: `picked !== null && !answered`; dopisek statusu: `answerStatus === "pending"` → „⏳ Zapisywanie…”, `"saved"` → obecny tekst „Odpowiedź jest OSTATECZNA…”, `"failed"` → „⚠️ Nie udało się zapisać odpowiedzi” (czerwony `#E8376B`).
- Licznik poprawnych: `Poprawne odpowiedzi: {correctTotal} / {totalQuestions}`.
- Przyciski odpowiedzi `disabled={answered || picked !== null}` bez zmian.

`Break.jsx` → `export default function Break({ participant, nextModule, isAdminPause })` — usuń kanał Realtime, poll 20 s, `onResume`, `sessionId`; zostaje animacja kropek i teksty. `WaitingResults.jsx` → `export default function WaitingResults({ participant })` — usuń kanał, poll 20 s i `onReveal`. (Faza przychodzi z hooka w App; te polle to martwy kod po 06-05 — RESEARCH „Do usunięcia”.)
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "timePerQ" src/screens/Quiz.jsx` → 0 linii (V-11)
    - `grep -n "setInterval" src/screens/Quiz.jsx src/screens/Break.jsx src/screens/WaitingResults.jsx` → tylko animacja kropek w Break/WaitingResults (≤ 2 linie, zawierające `setDots`)
    - `grep -n "supabase\|getSessionForCity\|getSessionById" src/screens/Break.jsx src/screens/WaitingResults.jsx` → 0 linii
    - `grep -n "Sprawdzamy odpowiedź" src/screens/Quiz.jsx` → 1 linia; `grep -n "Nie udało się zapisać odpowiedzi" src/screens/Quiz.jsx` → 1 linia
    - `grep -n "Pytanie {qNumInModule} / {moduleCount} · #{qNumGlobal}/{totalQuestions}" src/screens/Quiz.jsx` → 1 linia
  </acceptance_criteria>
  <done>Ekrany gry nie mają własnej logiki czasu ani subskrypcji.</done>
</task>

<task type="auto">
  <name>Task 2: App.jsx — routing uczestnika z useParticipantGame, localStorage, data-fue-*</name>
  <files>src/App.jsx</files>
  <read_first>
    - src/App.jsx (cały, 745 linii — zachować: podium broadcast 79–97, liveParams 99–103, startServerClock/keepRealtimeAlive 107–112, restore admina 121–124, routing welcome/code_entry/admin_login/practice/no_questions/admin/podium)
    - src/hooks/useParticipantGame.js, src/lib/participantState.js
    - src/screens/Lobby.jsx (linie 11–131: kiedy woła onStartQuiz)
  </read_first>
  <action>
Przebuduj ścieżkę uczestnika; ścieżki admina/podium/LiveView/praktyki bez zmian funkcjonalnych.
1. Usuń stan i logikę starej maszyny: `quizSession, cityQuestions, currentMod, qIdx, timer, picked, answered, answeredRef, myPts, allAnswers, nextModule, countdownNum, revealAns, BREAK_AFTER, timerRef, pickTime, pickedRef, qStartedAtRef, modTimePerQRef, currentModRef, qIdxRef, cityQuestionsRef, quizChRef, advFallbackRef, broadcastSession, getQuestionState, syncToSession`, efekty timera (176–198), odliczania (206–220), kanału sesji (226–381), `recordAnswer, handleTimeout, handlePick, armAdvanceFallback, advanceQuestion, handleResumeFromBreak, startQuiz`, gałęzie routingu `break, admin_pause, waiting_results, countdown, module_intro, quiz` oraz importy `ModuleIntro`, `submitAnswer, getSessionForCity, getSessionById, getQuestions, updateSession, advanceSessionQuestion, getParticipantAnswers`, `getModule, REVEAL_SECONDS, MODULE_INTRO_SECONDS, remainingSeconds, advanceLeadSeconds, fallbackJitterMs`, `serverNow`.
2. `const [participant, setParticipant] = useState(() => loadParticipant());` — przy starcie (po `loading` auth, gdy nie admin) jeśli `participant` jest → `setScreen("game")`; `fue_bg` z localStorage (`BG_KEY`) ustawiane w `--fue-bg` od razu.
3. `const game = useParticipantGame(screen === "game" ? participant : null);`
4. `handleCodeSuccess(p)` → `saveParticipant(p); setParticipant(p); setScreen("game");`.
5. Efekt: gdy `game.session?.id` zmieni się i `participant?.code` → `markCodeUsed(participant.code, game.session.id)` (raz na id — ref). Efekt tła: z `game.session` (`bg`/`bg_mobile` wg `isDesktop` jak dotąd) albo `getCityBg(participant.city)` gdy brak sesji; zapis do `localStorage[BG_KEY]`.
6. Efekt `data-fue-*` (sonda 06-08): gdy `screen === "game"`: `document.body.dataset.fuePhase = view.phase` (mapuj `no_session` → `lobby`), `fueQ = view.idx != null ? String(view.idx + 1) : ""`, `fueRemaining = String(view.secondsLeft ?? "")`, `fueLocked = myAnswers[view.item?.id] ? "1" : "0"`, `fueChoice = litera A–D lub ""`; poza grą usuń te atrybuty (`delete document.body.dataset.fuePhase` itd.).
7. Routing `screen === "game"` (`switch (view.phase)`), `mod = MODULES.find((m) => m.id === view.item?.m)` (tylko nazwa/ikona/kolor):
   - `loadState === "invalid_code"` → `clearParticipant(); setScreen("code_entry")` (w efekcie).
   - `loadState === "loading"` i brak `game.plan` i brak `game.session` → dotychczasowy ekran „Ładowanie…” (06-07 zamieni na szkielet).
   - `lobby` / `no_session` → `<Lobby participant isDesktop isPractice={!!session?.is_practice} onStartQuiz={() => game.refresh()} onPractice={() => setScreen("practice")} />`.
   - `intro` → `<ModuleIntroFS mod={mod} secondsLeft={view.secondsLeft} />`.
   - `countdown` → `<Countdown num={Math.max(0, view.secondsLeft - 1)} />` (3-2-1-START jak dotąd).
   - `quiz` / `reveal` → `<Quiz item={view.item} mod={mod} phase={view.phase} secondsLeft={view.secondsLeft} opensAt closesAt revealUntil picked={my?.chosen ?? null} answerStatus={my?.status ?? null} correctAns={view.phase === "reveal" ? game.revealAns : null} qNumGlobal={view.idx + 1} totalQuestions={plan.length} qNumInModule={1 + plan.filter((x) => x.m === view.item.m && x.i < view.item.i).length} moduleCount={plan.filter((x) => x.m === view.item.m).length} correctTotal={game.correctTotal} isDesktop isPractice participantCode sessionId onPick={game.pick} />`.
   - `paused` → `<Break participant nextModule={view.item?.m} isAdminPause />`.
   - `finished` → `<WaitingResults participant />`.
   - `results` / `ended` → dla `session.is_practice && phase === "ended"` → Lobby (jak dotąd: praktyka wraca do poczekalni); inaczej `<Ended participant myPts={0} allAnswers={plan-mapped} isPractice onGoHome={resetApp} />`, gdzie `allAnswers = Object.entries(myAnswers).map(([qId, a]) => ({ qId, module: plan?.find((x) => x.id === qId)?.m, picked: a.chosen, correct: a.correct === true, pts: 0 }))`.
   - `legacy` → ekran informacyjny (styl jak `no_questions`): „Ten quiz został uruchomiony w starszej wersji panelu. Organizator musi zakończyć sesję i uruchomić ją ponownie z odświeżonego panelu.” — nowy klient nie odtwarza starej maszyny stanów (świadoma decyzja: nie wdrażać frontu w trakcie wydarzenia; RESEARCH „Runtime State Inventory”).
8. `resetApp` → `clearParticipant(); document.documentElement.style.removeProperty("--fue-bg"); setParticipant(null); setScreen("welcome");`.
  </action>
  <verify>
    <automated>npx vitest run && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "timePerQ" src/App.jsx` → 0 linii (V-11)
    - `grep -n "advanceSessionQuestion\|armAdvanceFallback\|fallbackJitterMs\|advanceLeadSeconds\|setInterval(tick\|sessionStorage" src/App.jsx` → 0 linii
    - `grep -n "useParticipantGame(" src/App.jsx` → 1 linia; `grep -c "dataset.fuePhase\|dataset.fueQ\|dataset.fueRemaining\|dataset.fueLocked" src/App.jsx` ≥ 4
    - `wc -l < src/App.jsx` < 500 (stara maszyna stanów usunięta)
    - `npm run build` kod 0; `npx vitest run` zielone
    - DEMO: `npm run build` z pustymi `VITE_SUPABASE_*` (`VITE_SUPABASE_URL= VITE_SUPABASE_ANON_KEY= npx vite build --outDir "<scratchpad>/dist-demo"`) kończy się kodem 0
  </acceptance_criteria>
  <done>Ekran uczestnika wynika wyłącznie z planu i zegara; refresh wraca bez kodu; sonda ma stabilne atrybuty.</done>
</task>

</tasks>

<verification>
`npx vitest run`, `npm run build`, build DEMO. `rg -n "timePerQ" src/screens/Quiz.jsx src/App.jsx` → brak (V-11). Zachowanie e2e — 06-08.
</verification>

<success_criteria>
- App.jsx: routing uczestnika = switch po fazie; brak starych timerów i kierowcy
- Quiz/Break/WaitingResults bez subskrypcji i bez timePerQ
</success_criteria>

<output>
After completion, create `.planning/phases/06-rozgrywka-autorytatywna-serwera-i-plynnosc-jak-w-aplikacji/06-05-SUMMARY.md`
</output>
