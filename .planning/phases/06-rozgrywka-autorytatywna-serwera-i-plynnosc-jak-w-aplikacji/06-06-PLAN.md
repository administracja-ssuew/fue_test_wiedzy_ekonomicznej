---
phase: 06-rozgrywka-autorytatywna-serwera-i-plynnosc-jak-w-aplikacji
plan: 06
type: execute
wave: 3
depends_on: ["06-04"]
files_modified:
  - src/hooks/useLiveProjection.js
  - src/screens/AdminPanel.jsx
autonomous: true
requirements: [P6-SC1, P6-SC4, P6-SC5]

must_haves:
  truths:
    - "LiveView i podgląd admina liczą fazę i czas z planu sesji (tpq z planu), dla sesji z planem"
    - "Publiczny LiveView dostaje poprawną odpowiedź i licznik poprawnych dopiero po closes_at + 1,5 s (get_answer_summary_v2)"
    - "Start z panelu tworzy plan (start_quiz_session_v2); pauza/wznowienie/Następne/Powtórz to jedno przesunięcie kotwicy w RPC"
    - "Panel nie zapisuje przejść pytań dla sesji z planem — robi to zamiatacz"
    - "Panel pokazuje czerwony baner i przejmuje przejścia awaryjnie (admin_sweep_session co 2 s), gdy zamiatacz milczy > 5 s"
    - "„Ogłoś wyniki” jest dostępne także w fazie finished (bez wymuszania pauzy)"
  artifacts:
    - path: "src/hooks/useLiveProjection.js"
      provides: "Projekcja plan-aware (legacy zostaje dla sesji bez planu)"
      contains: "projectPlanState"
    - path: "src/screens/AdminPanel.jsx"
      provides: "Akcje v2, baner zamiatacza, brak zapisu przejść dla planu"
      contains: "startQuizSessionV2"
  key_links:
    - from: "src/hooks/useLiveProjection.js"
      to: "getSessionPlan / getAnswerSummaryV2"
      via: "pobranie planu raz na session.id; reveal po bramce"
      pattern: "getAnswerSummaryV2\\("
    - from: "src/screens/AdminPanel.jsx"
      to: "adminSkipQuestion"
      via: "goToNextQuestion dla sesji z planem"
      pattern: "adminSkipQuestion\\("
---

<objective>
Przepiąć podgląd (LiveView + embed admina) i sterowanie w panelu na plan sesji: projekcja z planu, reveal przez v2, akcje admina v2, usunięcie zapisu przejść z `driverTick` dla sesji z planem, baner i zapas przy martwym zamiataczu.

Purpose: Decyzje CONTEXT „Przejścia” (kierowca w AdminPanel do usunięcia; baza wykonuje przejścia), „⏭ Następne zostaje jako przesunięcie planu”, „Model czasu” (LiveView/podgląd admina = funkcja planu). SC1 (karta admina niepotrzebna), SC4, SC5.
Output: zmodyfikowane `src/hooks/useLiveProjection.js` i `src/screens/AdminPanel.jsx`. `LiveView.jsx` bez zmian (czyta `timePerQ` z hooka, który teraz daje tpq z planu).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-rozgrywka-autorytatywna-serwera-i-plynnosc-jak-w-aplikacji/06-CONTEXT.md
@.planning/phases/06-rozgrywka-autorytatywna-serwera-i-plynnosc-jak-w-aplikacji/06-04-SUMMARY.md
@src/hooks/useLiveProjection.js

<interfaces>
Z src/lib/plan.js: `toMs, planPosition, projectPlanState, REVEAL_GATE_MS`.
Z src/lib/supabase.js (06-04):
```js
startQuizSessionV2(id) → { ok, reason, session, error }
adminPauseSession(id) / adminResumeSession(id) → { ok, reason, session, error }
adminSkipQuestion(id, idx) / adminRepeatQuestion(id, idx) → { ok, reason, session, error }  // reason "noop" = drugi klik
getSessionPlan(id) → items | null
getAnswerSummaryV2(sessionId, questionId) → { total, correct, ans }
getSweeperStatus() → { cron_installed, job_active, schedule, last_run_age_s, last_status } | null
adminSweepSession(id) → { error }
```
useLiveProjection zwraca dziś (kontrakt dla LiveView.jsx i embedu — NIE zmieniać nazw):
`{ phase ("waiting"|"paused"|"quiz"|"reveal"), gIdx, timer, autoSec, cdNum, firstOfModule, currentQ, questions, mod, timePerQ, reveal, revealTotal, revealCorrect, revealAns, liveCount, participantsTotal, bg, podium }`.
AdminPanel (SesjaTab): `session`, `sessionRef`, `setSession`, `upd(updates)`, `quizBcChRef` (broadcast `quiz_event`), `cityQuestions`, `driverTick` (linie 653–756), `goToNextQuestion` (968–974), przyciski Start/Pauza/Następne/Zakończ/Wznów/Ogłoś/Powtórz (1069–1120), embed `useLiveProjection(city, { detailed: true })` (1467).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: useLiveProjection — projekcja z planu + reveal v2 po bramce</name>
  <files>src/hooks/useLiveProjection.js</files>
  <read_first>
    - src/hooks/useLiveProjection.js (cały)
    - src/screens/LiveView.jsx (jak konsumuje phase/cdNum/firstOfModule/timer/timePerQ/autoSec/revealAns)
    - src/lib/plan.js
  </read_first>
  <action>
1. Stan: `planRef = useRef(null)`, `planSidRef = useRef(null)`, `[planTpq, setPlanTpq] = useState(null)`. W `apply(s)` i przy initial load: gdy `s.plan_anchor_at` i `planSidRef.current !== s.id` → `planSidRef.current = s.id; getSessionPlan(s.id).then((items) => { planRef.current = items; })`. Sesja bez planu → `planRef.current = null`.
2. W `tick`: jeśli `planRef.current?.length && sessionRef.current?.plan_anchor_at` → `const v = projectPlanState({ items: planRef.current, anchorMs: toMs(s.plan_anchor_at), pausedAtMs: toMs(s.plan_paused_at), status: s.status, nowMs: serverNow() })` i mapuj na stary kontrakt:
   - `lobby`/`results`/`ended`/`legacy` → `phase "waiting"`;
   - `intro`/`countdown` → `phase "quiz"`, `cdNum = Math.max(0, v.secondsLeft - 1)`, `firstOfModule = v.firstOfModule`, `timer = v.item.tpq`;
   - `quiz` → `phase "quiz"`, `cdNum = null`, `timer = v.secondsLeft`;
   - `reveal` → `phase "reveal"`, `autoSec = v.secondsLeft`;
   - `finished` → `phase "reveal"`, `autoSec = 0` (≤ 1 s do status results — bez mignięcia);
   - `paused` → `phase "paused"`.
   `idx` = indeks pytania w `questionsRef.current` o `id === v.item.id` (fallback `v.idx`); `setPlanTpq(v.item?.tpq ?? null)`.
   Reveal: pobieraj, gdy `serverNow() >= v.closesAt + REVEAL_GATE_MS + 100` (raz na idx, zamiast `setTimeout 1500`); `detailed` → `getLiveQuestionStats` jak dziś; publiczny → `getAnswerSummaryV2(sid, q.id)`; gdy `ans == null` mimo bramki → ponów raz po 1 s.
   Brak planu → dotychczasowa gałąź `projectLiveState` bez zmian (sesje legacy do 06-11).
3. Zwracane `timePerQ = planTpq ?? (mod?.timePerQ || 60)` — dla sesji z planem czas z planu (SC4); fallback modułu tylko dla legacy. Komentarz: fallback do usunięcia w 06-11.
Kontrakt zwracanych pól bez zmian nazw.
  </action>
  <verify>
    <automated>npx vitest run && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "projectPlanState(" src/hooks/useLiveProjection.js` ≥ 1; `grep -c "getSessionPlan(" src/hooks/useLiveProjection.js` ≥ 1; `grep -c "getAnswerSummaryV2(" src/hooks/useLiveProjection.js` ≥ 1
    - `grep -n "REVEAL_GATE_MS" src/hooks/useLiveProjection.js` → ≥ 1 linia
    - `grep -n "timePerQ" src/hooks/useLiveProjection.js` → wyłącznie linie gałęzi legacy/fallbacku (każda linia z `timePerQ` zawiera też `planTpq` albo leży w gałęzi `projectLiveState`)
    - Zwracany obiekt nadal ma klucze `phase, gIdx, timer, autoSec, cdNum, firstOfModule, currentQ, questions, mod, timePerQ, reveal, revealTotal, revealCorrect, revealAns, liveCount, participantsTotal, bg, podium`
    - `npm run build` kod 0
  </acceptance_criteria>
  <done>LiveView i embed liczą z planu; poprawna odpowiedź na projektorze dopiero po bramce.</done>
</task>

<task type="auto">
  <name>Task 2: AdminPanel — akcje v2, brak zapisu przejść dla planu, baner zamiatacza, wyniki w finished</name>
  <files>src/screens/AdminPanel.jsx</files>
  <read_first>
    - src/screens/AdminPanel.jsx (linie 1–20 importy; 504–600 stan SesjaTab i load; 640–760 driverTick; 780–840 kanały + upd; 955–975 curQuestionTimePerQ/goToNextQuestion; 1060–1125 przyciski; 1216–1270 wyniki/podium)
    - src/lib/plan.js, src/lib/supabase.js (wrappery z 06-04)
  </read_first>
  <action>
1. Importy: dodaj `startQuizSessionV2, adminPauseSession, adminResumeSession, adminSkipQuestion, adminRepeatQuestion, getSessionPlan, getSweeperStatus, adminSweepSession` z `../lib/supabase.js`; `planPosition, toMs` z `../lib/plan.js`.
2. Plan w SesjaTab: `const [plan, setPlan] = useState(null); const planRef = useRef(null);` — efekt na `[session?.id, session?.plan_anchor_at ? 1 : 0]`: gdy jest kotwica → `getSessionPlan(session.id).then((it) => { planRef.current = it; setPlan(it); })`, inaczej null. Pomocnik `const planPos = () => (planRef.current && sessionRef.current?.plan_anchor_at) ? planPosition(planRef.current, toMs(sessionRef.current.plan_anchor_at), toMs(sessionRef.current.plan_paused_at), serverNow()) : null;`. `const isPlan = !!session?.plan_anchor_at;`
3. `applyV2(res, logType)` — wspólna obsługa wyników v2: `res.error` → `alert("Błąd: " + res.error)`; `!res.ok && res.reason !== "noop"` → `alert("Nie wykonano: " + res.reason)`; `res.session` → `sessionRef.current = res.session; setSession(res.session)`; broadcast `quiz_event` przez `quizBcChRef.current` (payload = sesja; to tylko SYGNAŁ); `logEvent({ type: logType, sessionId, city, actor: adminId, detail: { idx: res.session?.current_question_idx } })`.
4. Start (1069–1081): `startQuizSessionV2(session.id)` → `applyV2(res, "session_started")`. Stary `startQuizSession` przestaje być wołany (pozostaje eksport do 06-11). Gdy `res.error` zawiera „sekcji 39” → alert z tym tekstem (świadomie brak cichego fallbacku do sesji bez planu).
5. Pauza: `isPlan ? applyV2(await adminPauseSession(session.id), "session_paused") : <istniejące upd(...)>`. Wznów: `isPlan ? adminResumeSession : <istniejące>`. Powtórz: `isPlan ? adminRepeatQuestion(session.id, planPos()?.idx) : <istniejące>`. `goToNextQuestion(tpqOverride)`: `isPlan ? applyV2(await adminSkipQuestion(session.id, planPos()?.idx), "question_skipped") : <istniejące cofanie q_started_at>`. Gałęzie legacy zostają (usunie je 06-11 po wdrożeniu) — oznacz komentarzem `// LEGACY (sesje bez planu) — usunąć w 06-11`.
6. `driverTick`: na początku po odczycie `s`: dla sesji z planem `const pos = planPos();` → `idx = pos?.idx ?? s.current_question_idx`, `q = cityQuestions.find((x) => x.id === pos?.item?.id) ?? cityQuestions[idx]`, `tpqNow = pos?.item?.tpq ?? <dotychczasowe z MODULES>`, `startedMsNow = pos?.opensAt ?? <dotychczasowe>`. Licznik odpowiedzi i `shouldEndEarly` bez zmian (auto-skip → `goToNextRef.current(tpqNow)` → `adminSkipQuestion`; dla TWE ≤ 20 s i tak wyłączony — decyzja „Auto-skip opcjonalny”). Tuż przed blokiem `// ── KIEROWCA PRZEJŚCIA PYTANIA` dodaj `if (s.plan_anchor_at) return; // przejścia sesji z planem wykonuje zamiatacz w bazie (faza 6)`. `curQuestionTimePerQ` (955–962) → dla planu `planPos()?.item?.tpq`.
7. Baner zamiatacza: efekt przy `st ∈ {running, paused}` i `isPlan`: co 10 s `getSweeperStatus()` → `stale = !st || !st.job_active || st.last_run_age_s == null || st.last_run_age_s > 5` → `setSweeperStale(stale ? (st?.last_run_age_s ?? null) : false)`; gdy `sweeperStale !== false` i `st === "running"` → interwał 2 s `adminSweepSession(session.id)` (Pułapka 9 — zapas). Render nad przyciskami: czerwony pasek (`background: "rgba(232,55,107,.15)", border: "1px solid rgba(232,55,107,.5)", color: "#E8376B"`) z tekstem „⚠️ Zamiatacz serwera nie odpowiada ({N} s) — panel przesuwa pytania awaryjnie. Nie zamykaj tej karty.”.
8. „🏆 Ogłoś wyniki”: dodatkowo przy `st === "running" && isPlan && planPos()?.phase === "finished"` (gdy zamiatacz nie zdążył) — ten sam `upd({ status: "results" })`. Istniejący przycisk w `paused` bez zmian.
  </action>
  <verify>
    <automated>npx vitest run && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "startQuizSessionV2(\|adminPauseSession(\|adminResumeSession(\|adminSkipQuestion(\|adminRepeatQuestion(" src/screens/AdminPanel.jsx` ≥ 5
    - `grep -n "if (s.plan_anchor_at) return;" src/screens/AdminPanel.jsx` → 1 linia, położona przed linią `advanceSessionQuestion(s.id, idx, nextIdx, lead)`
    - `grep -n "startQuizSession(session.id)" src/screens/AdminPanel.jsx` → 0 linii
    - `grep -n "Zamiatacz serwera nie odpowiada" src/screens/AdminPanel.jsx` → 1 linia; `grep -c "adminSweepSession(" src/screens/AdminPanel.jsx` ≥ 1; `grep -c "getSweeperStatus(" src/screens/AdminPanel.jsx` ≥ 1
    - `grep -c "LEGACY (sesje bez planu)" src/screens/AdminPanel.jsx` ≥ 3
    - `npm run build` kod 0; `npx vitest run` zielone
  </acceptance_criteria>
  <done>Panel steruje planem jednym zapisem RPC; zamknięcie karty admina nie zatrzymuje gry; martwy zamiatacz jest widoczny i ma zapas.</done>
</task>

</tasks>

<verification>
`npx vitest run` + `npm run build`. Scenariusz „admin zamyka przeglądarkę” — sonda `PROBE_ADMIN_EXIT=1` w 06-08.
</verification>

<success_criteria>
- LiveView/embed z planu, reveal po bramce
- Wszystkie akcje sterujące dla sesji z planem idą przez RPC v2; brak zapisu przejść z panelu
</success_criteria>

<output>
After completion, create `.planning/phases/06-rozgrywka-autorytatywna-serwera-i-plynnosc-jak-w-aplikacji/06-06-SUMMARY.md`
</output>
