---
phase: 06-rozgrywka-autorytatywna-serwera-i-plynnosc-jak-w-aplikacji
plan: 07
type: execute
wave: 4
depends_on: ["06-05"]
files_modified:
  - src/hooks/useWakeLock.js
  - src/styles/global.css
  - src/hooks/useParticipantGame.js
  - src/App.jsx
  - src/screens/Quiz.jsx
autonomous: true
requirements: [P6-FLUID]

must_haves:
  truths:
    - "Ekran telefonu nie gaśnie w lobby i w quizie (Wake Lock ponawiany po powrocie karty)"
    - "Pasek i pierścień czasu animuje kompozytor (CSS z ujemnym animation-delay z terminu), niezależnie od wątku JS"
    - "Wybór odpowiedzi jest widoczny w tej samej klatce, z wibracją 15 ms tam, gdzie działa"
    - "Zmiana fazy/pytania przechodzi przez View Transitions, gdy dostępne i brak prefers-reduced-motion"
    - "Treść następnego pytania jest lokalnie dostępna przed jego otwarciem (cały plan z q/opts przychodzi w snapshocie — osobny prefetch pytań zbędny); tło sesji jest wczytane z wyprzedzeniem"
    - "Refresh bez cache pokazuje szkielet ekranu pytania zamiast pustki; prefers-reduced-motion wyłącza wszystkie animacje"
  artifacts:
    - path: "src/hooks/useWakeLock.js"
      provides: "Screen Wake Lock z ponawianiem przy visibilitychange"
    - path: "src/styles/global.css"
      provides: "@keyframes fueDrain/fueRing, reguły prefers-reduced-motion, ::view-transition"
      contains: "fueDrain"
    - path: "src/screens/Quiz.jsx"
      provides: "Pasek/pierścień CSS zakotwiczony w terminie, vibrate, statusy zapisu"
      contains: "animationDelay"
  key_links:
    - from: "src/App.jsx"
      to: "src/hooks/useWakeLock.js"
      via: "useWakeLock(aktywny w fazach gry)"
      pattern: "useWakeLock\\("
    - from: "src/hooks/useParticipantGame.js"
      to: "document.startViewTransition"
      via: "flushSync przy zmianie fazy/idx"
      pattern: "startViewTransition"
---

<objective>
Płynność „jak w aplikacji” wyłącznie na natywnych API (ograniczenie projektu: bez bibliotek UI, style inline + keyframes w global.css): Wake Lock, pasek/pierścień czasu jako animacja CSS zakotwiczona w terminie, cyfry z rAF (już w hooku), optimistic lock-in + vibrate, View Transitions, prefetch następnego pytania, szkielet przy refreshu, `prefers-reduced-motion`.

Purpose: Decyzja CONTEXT „Płynność” — wszystkie 8 punktów.
Output: `src/hooks/useWakeLock.js` (nowy), zmiany w `global.css`, `useParticipantGame.js`, `App.jsx`, `Quiz.jsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-rozgrywka-autorytatywna-serwera-i-plynnosc-jak-w-aplikacji/06-CONTEXT.md
@.planning/research/PLYNNOSC-ROZGRYWKI.md
@.planning/phases/06-rozgrywka-autorytatywna-serwera-i-plynnosc-jak-w-aplikacji/06-05-SUMMARY.md

<interfaces>
Z useParticipantGame (06-04): `view = { phase, idx, item, opensAt, closesAt, revealUntil, secondsLeft, ... }`, `plan`, `session`, `myAnswers`, `pick`, `refresh`, `loadState`; wewnętrzny ticker rAF z `setView` tylko przy zmianie klucza `${phase}|${idx}|${secondsLeft}|${opensAt}`.
Z Quiz.jsx (06-05): propsy `item, phase, secondsLeft, opensAt, closesAt, revealUntil, picked, answerStatus, correctAns, onPick, ...`; SVG `width="54"` z pierścieniem i `<span>` z cyframi (sonda używa tego jako fallbacku).
Z serverClock: `serverNow()`.
React 18: `import { flushSync } from "react-dom";`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: useWakeLock + global.css (keyframes, reduced-motion, view-transition) + View Transitions i prefetch w hooku</name>
  <files>src/hooks/useWakeLock.js, src/styles/global.css, src/hooks/useParticipantGame.js, src/App.jsx</files>
  <read_first>
    - src/styles/global.css (istniejące keyframes su/pi/fi/…)
    - src/hooks/useParticipantGame.js (ticker rAF i miejsce `setView`)
    - src/App.jsx (routing gry z 06-05)
    - .planning/phases/06-rozgrywka-autorytatywna-serwera-i-plynnosc-jak-w-aplikacji/06-RESEARCH.md (Przykład „Wake Lock z ponawianiem”, Pułapka 10)
  </read_first>
  <action>
1. `src/hooks/useWakeLock.js` — `export default function useWakeLock(active)` dokładnie wg wzorca z RESEARCH: `if (!active || !("wakeLock" in navigator)) return;`, `acquire` (pomija gdy `document.hidden`, try/catch cichy), listener `visibilitychange` ponawia przy powrocie (blokada zwalnia się przy ukryciu karty), cleanup `lock?.release?.().catch(() => {})`. Komentarz: iOS PWA z ekranu głównego dopiero od 18.4 — brak wsparcia = cicho pomijamy.
2. `src/App.jsx`: `useWakeLock(screen === "game" && ["lobby","no_session","intro","countdown","quiz","reveal","paused","finished"].includes(game.view.phase));`.
3. `src/styles/global.css` dopisz:
```css
@keyframes fueDrain { from { transform: scaleX(1) } to { transform: scaleX(0) } }
@keyframes fueRing  { from { stroke-dashoffset: 0 } to { stroke-dashoffset: var(--fue-circ) } }
::view-transition-old(root), ::view-transition-new(root) { animation-duration: .18s; }
@media (prefers-reduced-motion: reduce) {
  .fue-drain, .fue-ring { animation: none !important; }
  *, *::before, *::after { animation-duration: .001ms !important; animation-iteration-count: 1 !important; transition-duration: .001ms !important; }
  ::view-transition-group(*), ::view-transition-old(*), ::view-transition-new(*) { animation: none !important; }
}
```
4. `useParticipantGame.js` — View Transitions: w tickerze, gdy zmienia się `phase` lub `idx` (nie przy samej zmianie `secondsLeft`), a `"startViewTransition" in document` i `!window.matchMedia("(prefers-reduced-motion: reduce)").matches` i `!document.hidden` → `document.startViewTransition(() => flushSync(() => setView(next)))`; inaczej zwykłe `setView(next)` (Pułapka 10: w React 18 bez flushSync zrzut „po” powstaje przed renderem). Prefetch (punkt „prefetch następnego pytania” z CONTEXT): treść wszystkich pytań (`q`/`opts`) jest już w planie ze snapshotu i w cache localStorage, więc NIE dodawaj żadnego pobierania pytań. Jedyny prefetch = zasoby graficzne: raz na `session.id`, jeśli `session.bg`/`bg_mobile` zawiera `url(`, wyciągnij URL-e i `new Image().src = url` (tło gotowe przed pierwszym ekranem modułu / po zmianie sesji). Komentarz po polsku wyjaśniający, dlaczego to wystarcza.
  </action>
  <verify>
    <automated>npx vitest run && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "navigator.wakeLock.request(\"screen\")" src/hooks/useWakeLock.js` → 1 linia; `grep -c "visibilitychange" src/hooks/useWakeLock.js` ≥ 2
    - `grep -n "useWakeLock(" src/App.jsx` → 1 linia
    - `grep -c "@keyframes fueDrain\|@keyframes fueRing\|prefers-reduced-motion" src/styles/global.css` ≥ 3
    - `grep -n "startViewTransition" src/hooks/useParticipantGame.js` → ≥ 1 linia i ten sam plik zawiera `flushSync` oraz `prefers-reduced-motion`
    - `npm run build` kod 0
  </acceptance_criteria>
  <done>Wake Lock, przejścia i prefetch działają bez żadnej zależności.</done>
</task>

<task type="auto">
  <name>Task 2: Quiz.jsx — pasek i pierścień CSS zakotwiczone w terminie, vibrate, szkielet w App</name>
  <files>src/screens/Quiz.jsx, src/App.jsx</files>
  <read_first>
    - src/screens/Quiz.jsx (po 06-05 — pierścień SVG r=22, `timerPct`, pasek postępu globalnego, pasek potwierdzenia)
    - src/App.jsx (gałąź `loadState === "loading"` z 06-05)
    - .planning/phases/06-rozgrywka-autorytatywna-serwera-i-plynnosc-jak-w-aplikacji/06-RESEARCH.md (Przykład „Pasek czasu zakotwiczony w terminie”)
  </read_first>
  <action>
1. Pasek czasu (nowy, pod paskiem górnym, nad postępem globalnym), tylko w fazie quiz/reveal:
```jsx
<div style={{ height: 6, background: "rgba(255,255,255,.07)", overflow: "hidden", flexShrink: 0 }}>
  <div key={`${item.id}-${opensAt}`} className="fue-drain" style={{
    height: "100%", transformOrigin: "left", background: `linear-gradient(90deg,${mod.color},#F5C518)`,
    animationName: "fueDrain", animationTimingFunction: "linear", animationFillMode: "forwards",
    animationDuration: `${item.tpq}s`, animationDelay: `${-(serverNow() - opensAt) / 1000}s`,
    animationPlayState: phase === "quiz" ? "running" : "paused",
    transform: phase === "reveal" ? "scaleX(0)" : undefined }} />
</div>
```
   `key` restartuje animację przy skip/repeat (nowy `opensAt`). Import `serverNow` z `../lib/serverClock.js`.
2. Pierścień SVG: zostaw `svg width="54"` i `<span>` z cyframi `secondsLeft` (cyfry pochodzą z tickera rAF hooka — decyzja „cyfry z requestAnimationFrame + serverNow()”). Kółko postępu: `className="fue-ring"`, `key={`${item.id}-${opensAt}`}`, `strokeDasharray={circ}`, `style={{ "--fue-circ": circ, animationName: "fueRing", animationTimingFunction: "linear", animationFillMode: "forwards", animationDuration: `${item.tpq}s`, animationDelay: `${-(serverNow() - opensAt) / 1000}s`, animationPlayState: phase === "quiz" ? "running" : "paused", transition: "stroke .4s" }}` — usuń `strokeDashoffset` liczone z `timerPct` i `transition: stroke-dashoffset .95s` (skoki po zadławieniu JS). Kolor progów zostaje z `secondsLeft/item.tpq`.
3. Optimistic lock-in: `onClick={() => { if (answered || picked !== null) return; try { navigator.vibrate?.(15); } catch (_) {} onPick(i); }}` — stan „wybrano” pochodzi z `picked` ustawionego przez hook w tej samej klatce. W trakcie `answerStatus === "pending"` obramowanie wybranej odpowiedzi pulsuje (`animation: "pulse 1s infinite"` — keyframes `pulse` istnieje).
4. `App.jsx` — szkielet: gałąź `loadState === "loading"` bez planu/sesji ORAZ faza `plan_loading` zamiast napisu „Ładowanie…” renderują komponent-wartość `QuizSkeleton` (inline w App.jsx lub `src/components/QuizSkeleton.jsx` — wtedy dopisz do SUMMARY): pasek górny 54 px, blok pytania 2 linie, siatka 2×2 kafli `minHeight: 100` w kolorach `rgba(255,255,255,.06)` z `animation: "pulse 1.4s infinite"`; tło `var(--fue-bg)` (ustawione z `fue_bg` zanim przyjdzie snapshot).
  </action>
  <verify>
    <automated>npx vitest run && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "animationDelay" src/screens/Quiz.jsx` ≥ 2; `grep -c "fueDrain\|fueRing" src/screens/Quiz.jsx` ≥ 2
    - `grep -n "stroke-dashoffset .95s" src/screens/Quiz.jsx` → 0 linii
    - `grep -n "navigator.vibrate" src/screens/Quiz.jsx` → 1 linia
    - `grep -n "svg width=\"54\"" src/screens/Quiz.jsx` → 1 linia (fallback sondy zachowany)
    - `grep -n "timePerQ\|setInterval" src/screens/Quiz.jsx` → 0 linii
    - `grep -n "Skeleton" src/App.jsx` → ≥ 1 linia; gałąź `plan_loading` renderuje ten sam szkielet
    - `grep -n "new Image()" src/hooks/useParticipantGame.js` → ≥ 1 linia; w tym pliku brak dodatkowego `refresh(` wywoływanego w fazie reveal
    - `npm run build` kod 0
  </acceptance_criteria>
  <done>Licznik płynny niezależnie od wątku JS; dotyk potwierdzany natychmiast; refresh bez pustego ekranu.</done>
</task>

</tasks>

<verification>
`npx vitest run` + `npm run build`. Weryfikacja na realnych urządzeniach (Wake Lock, vibrate, View Transitions, reduced-motion) — checkpoint w 06-09.
</verification>

<success_criteria>
- 8 punktów płynności z CONTEXT zaimplementowanych natywnie, bez nowych zależności (`git diff package.json` bez zmian w dependencies)
</success_criteria>

<output>
After completion, create `.planning/phases/06-rozgrywka-autorytatywna-serwera-i-plynnosc-jak-w-aplikacji/06-07-SUMMARY.md`
</output>
