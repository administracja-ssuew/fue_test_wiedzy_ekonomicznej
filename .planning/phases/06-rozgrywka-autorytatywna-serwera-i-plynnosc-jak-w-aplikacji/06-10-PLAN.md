---
phase: 06-rozgrywka-autorytatywna-serwera-i-plynnosc-jak-w-aplikacji
plan: 10
type: execute
wave: 7
depends_on: ["06-09"]
files_modified:
  - SUPABASE_FIXES.sql
  - scripts/verify-prod.js
autonomous: false
requirements: [P6-SC5, P6-SC6]

must_haves:
  truths:
    - "Stary submit_answer nie zwraca is_correct/correct_ans przed closes_at + 1,5 s; dla sesji z planem deleguje do submit_answer_v2"
    - "get_participant_answers zwraca is_correct = NULL dla pytań jeszcze nieodsłoniętych"
    - "get_admin_answer_summary bramkuje correct i ans (plan dla sesji z planem; czas modułu dla legacy); admin widzi bez bramki"
    - "update_quiz_session_admin odrzuca zmiany rozgrywki (status running/paused, q_started_at, current_question_idx, pause_elapsed_s) dla sesji z planem"
    - "Stary start_quiz_session (ta sama sygnatura) odmawia startu komunikatem „przeładuj panel” — nieprzeładowana karta panelu nie uruchomi już sesji bez planu"
    - "Sygnatury wszystkich utwardzanych funkcji są identyczne jak przed zmianą (stary bundle nie dostaje błędów PGRST202)"
  artifacts:
    - path: "SUPABASE_FIXES.sql"
      provides: "Sekcja 41 — utwardzenie starych RPC po wdrożeniu frontu"
      contains: "─── 41."
    - path: "scripts/verify-prod.js"
      provides: "Sprawdzenie znacznika sekcji 41"
      contains: "schema_marker_41"
  key_links:
    - from: "public.submit_answer (stara sygnatura)"
      to: "public.submit_answer_v2"
      via: "delegacja dla sesji z planem"
      pattern: "submit_answer_v2\\("
---

<objective>
Sekcja 41 zamyka pozostałe wycieki poprawności w STARYCH RPC (Pułapka 1: `is_correct` z `submit_answer`, `get_participant_answers`, licznik `correct` w `get_admin_answer_summary`) i blokuje starą ścieżkę sterowania sesjami z planem. Wykonywane dopiero po bramce 06-09 (`ZGODA NA UTWARDZENIE: TAK`).

Purpose: SC5 dla każdego klienta (także starego bundla w cache telefonu), SC6 — te same sygnatury, degradacja starego klienta tylko kosmetyczna (brak koloru poprawnej w reveal).
Output: sekcja 41 w `SUPABASE_FIXES.sql`, sprawdzenie w `verify-prod.js`, wgranie ręczne + weryfikacja.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-rozgrywka-autorytatywna-serwera-i-plynnosc-jak-w-aplikacji/06-RESEARCH.md
@.planning/phases/06-rozgrywka-autorytatywna-serwera-i-plynnosc-jak-w-aplikacji/06-09-DEPLOY.md

<interfaces>
Stare sygnatury (NIE zmieniać — tylko CREATE OR REPLACE z identycznym zwrotem):
- `submit_answer(p_session_id UUID, p_code TEXT, p_name TEXT, p_question_id UUID, p_chosen INT) RETURNS JSON` — §36, SUPABASE_FIXES.sql:1074–1111
- `get_participant_answers(p_session_id UUID, p_code TEXT) RETURNS TABLE (question_id UUID, module INT, chosen INT, is_correct BOOLEAN, points INT)` — §16, :328–346
- `get_admin_answer_summary(p_session_id UUID, p_question_id UUID) RETURNS JSON {total, correct, ans}` — §29.4, :787–817
- `update_quiz_session_admin(p_session_id UUID, p_data JSONB) RETURNS VOID` — §29.5, :823–834
Z sekcji 39: `submit_answer_v2(...)`, `get_answer_summary_v2(...)`, `plan_position(...)`, kolumny `plan_anchor_at`, `plan_paused_at`, tabela `session_plans`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sekcja 41 w SUPABASE_FIXES.sql + sprawdzenie w verify-prod</name>
  <files>SUPABASE_FIXES.sql, scripts/verify-prod.js</files>
  <read_first>
    - .planning/phases/06-rozgrywka-autorytatywna-serwera-i-plynnosc-jak-w-aplikacji/06-09-DEPLOY.md (MUSI zawierać `ZGODA NA UTWARDZENIE: TAK` — inaczej przerwij plan)
    - SUPABASE_FIXES.sql (§16 :328–346, §29.4 :787–817, §29.5 :823–834, §36 :1074–1111, sekcja 39)
    - scripts/verify-prod.js
  </read_first>
  <action>
Sprawdź najpierw `grep -c "ZGODA NA UTWARDZENIE: TAK" .../06-09-DEPLOY.md` = 1; inaczej STOP.
Dopisz przed stopką `Done.` nagłówek `-- ─── 41. UTWARDZENIE STARYCH RPC — po wdrożeniu frontu fazy 6 ──` (komentarz: wgrywać DOPIERO po wdrożeniu nowego frontu i zielonej sondzie; stary bundle traci tylko kolor poprawnej w reveal — `App.jsx:399` starego bundla ma fallback, który dla anona i tak daje NULL).
41.1 `submit_answer` (ta sama sygnatura, CREATE OR REPLACE): na początku `IF EXISTS (SELECT 1 FROM public.quiz_sessions WHERE id = p_session_id AND plan_anchor_at IS NOT NULL) THEN PERFORM public.submit_answer_v2(p_session_id, p_code, p_name, p_question_id, p_chosen); RETURN json_build_object('is_correct', NULL, 'correct_ans', NULL); END IF;` Dalej ciało §36 bez zmian walidacji i zapisu, a RETURN: `v_gate := v_started + (v_tpq || ' seconds')::interval + interval '1.5 seconds'; RETURN json_build_object('is_correct', CASE WHEN clock_timestamp() >= v_gate THEN v_is_correct END, 'correct_ans', CASE WHEN clock_timestamp() >= v_gate THEN v_ans END);`. Zachowaj REVOKE/GRANT z §36.
41.2 `get_participant_answers` (ten sam RETURNS TABLE; przepisz na `LANGUAGE sql STABLE SECURITY DEFINER`): `is_correct` = `CASE WHEN <odsłonięte> THEN a.is_correct ELSE NULL END`, gdzie odsłonięte = `s.status IN ('results','ended')` OR (sesja z planem AND `EXTRACT(epoch FROM (COALESCE(s.plan_paused_at, clock_timestamp()) - s.plan_anchor_at))*1000 >= (item->>'c')::BIGINT + 1500` dla pozycji pytania w `session_plans.items`) OR (sesja bez planu AND (`gidx < s.current_question_idx` OR (`gidx = s.current_question_idx` AND `clock_timestamp() >= s.q_started_at + tpq + 1.5 s`))) — `gidx`/`tpq` jak w §29.4 (`row_number() OVER (ORDER BY module, sort_order, id) - 1`, `COALESCE(modules.time_per_q, 60)`). GRANT anon, authenticated jak w §16.
41.3 `get_admin_answer_summary` (ten sam zwrot JSON): sesja z planem → `RETURN public.get_answer_summary_v2(p_session_id, p_question_id)` (plpgsql); legacy → ciało §29.4, ale `correct` bramkowane tym samym warunkiem co `ans`; rola admina (`COALESCE(get_my_role(),'') IN ('city_admin','superadmin')`) widzi bez bramki (licznik na żywo w panelu). UWAGA: `get_answer_summary_v2` dla sesji bez planu woła `get_admin_answer_summary` — pętli nie ma, bo 41.3 deleguje tylko przy `plan_anchor_at IS NOT NULL`.
41.4 `update_quiz_session_admin` (ta sama sygnatura): po sprawdzeniu roli (`COALESCE(public.get_my_role(), '') NOT IN (...)` → 'forbidden'): jeśli sesja ma `plan_anchor_at IS NOT NULL` AND (`p_data ? 'q_started_at'` OR `p_data ? 'current_question_idx'` OR `p_data ? 'pause_elapsed_s'` OR `p_data->>'status' IN ('running','paused')`) → `RAISE EXCEPTION 'session has plan — use admin_* v2 actions (reload admin panel)'`. `status` = 'results'/'ended' przepuszczane. `status = 'waiting'` → dodatkowo `plan_anchor_at = NULL, plan_paused_at = NULL, revealed_idx = NULL, revealed_ans = NULL`. Reszta jak §29.5.
41.5a `start_quiz_session(p_session_id UUID) RETURNS TIMESTAMPTZ` (ta sama sygnatura co §29.5/39.9, CREATE OR REPLACE, bez DROP): po sprawdzeniu roli jak w 39.9 → `RAISE EXCEPTION 'Nieaktualny panel admina — przeładuj stronę (Ctrl+F5) i uruchom quiz ponownie. Start bez planu jest wyłączony (faza 6).';` (ciało nie aktualizuje sesji). Komentarz: nowy panel (06-06) woła wyłącznie `start_quiz_session_v2`; stara karta panelu otwarta między wdrożeniem a sekcją 41 mogła uruchomić sesję bez planu, której nowy klient nie odtworzy (ekran „legacy”) — po 41 jest to niemożliwe, a stary panel pokaże treść wyjątku. Przed dopisaniem `grep -rn "start_quiz_session\b\|startQuizSession(" src scripts` — jeśli skrypt (np. `bot-runner.js`/`load-runner.js`) woła starą funkcję, odnotuj w SUMMARY, że po 41 wymaga `start_quiz_session_v2`. Zachowaj `REVOKE`/`GRANT ... TO authenticated` z §29.5.
41.5 Znacznik: `CREATE OR REPLACE FUNCTION public.schema_marker_41() RETURNS BOOLEAN LANGUAGE sql IMMUTABLE AS $$ SELECT true $$; REVOKE EXECUTE ... FROM PUBLIC; GRANT EXECUTE ... TO anon, authenticated;`.
`scripts/verify-prod.js`: blok „🔐 SEKCJA 41 (utwardzenie starych RPC)”: `rpc("schema_marker_41")` → `data === true` ok, `isMissing` → bad „→ uruchom sekcję 41 (po wdrożeniu frontu!)”; oraz ponownie istnienie `submit_answer`, `get_participant_answers`, `get_admin_answer_summary`, `start_quiz_session` (stare sygnatury — SC6; dla `start_quiz_session` wywołanie anonem ma zwrócić błąd inny niż PGRST202 — funkcja istnieje).
  </action>
  <verify>
    <automated>grep -c "─── 41\.\|schema_marker_41" SUPABASE_FIXES.sql</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "─── 41\." SUPABASE_FIXES.sql` → 1 linia przed stopką `Done.`
    - `awk '/─── 41\./,/Done. Verify/' SUPABASE_FIXES.sql | grep -c "CREATE OR REPLACE FUNCTION public\.\(submit_answer\|get_participant_answers\|get_admin_answer_summary\|update_quiz_session_admin\|start_quiz_session\|schema_marker_41\)("` → 6
    - `awk '/─── 41\./,/Done. Verify/' SUPABASE_FIXES.sql | grep -c "przeładuj stronę"` ≥ 1 (blokada starego start_quiz_session w sekcji 41)
    - `awk '/─── 41\./,/Done. Verify/' SUPABASE_FIXES.sql | grep -c "DROP FUNCTION"` → 0 (sygnatury bez zmian)
    - `awk '/─── 41\./,/Done. Verify/' SUPABASE_FIXES.sql | grep -c "submit_answer_v2(\|get_answer_summary_v2("` ≥ 2
    - `grep -c "schema_marker_41" scripts/verify-prod.js` ≥ 1
    - `git diff --numstat HEAD -- SUPABASE_FIXES.sql` → druga kolumna (usunięte linie) = 0 (wykrywa też usunięte komentarze SQL)
  </acceptance_criteria>
  <done>Sekcja 41 gotowa do wgrania.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Ręczne wgranie sekcji 41 + automatyczna weryfikacja</name>
  <files>SUPABASE_FIXES.sql</files>
  <read_first>
    - SUPABASE_FIXES.sql (sekcja 41)
  </read_first>
  <action>
Claude wycina sekcję 41 (od `-- ─── 41.` do linii przed stopką `Done.`) do pliku `sekcja-41.sql` w scratchpadzie i podaje ścieżkę. Po wznowieniu uruchamia: `npm run verify-prod`, `npm run build`, w tle `npx vite preview --port 4173 --strictPort`, potem `PROBE_TARGET=prod PROBE_CONFIRM=1 npm run sonda` (tryb podstawowy — asercja SC5 musi przejść także z utwardzonymi starymi RPC) i zapisuje wyniki w SUMMARY.
  </action>
  <what-built>Sekcja 41 (utwardzenie submit_answer, get_participant_answers, get_admin_answer_summary, update_quiz_session_admin, blokada starego start_quiz_session + znacznik).</what-built>
  <how-to-verify>
    1. Supabase Dashboard → projekt `ytbwmmqwbfcugouourih` → SQL Editor.
    2. Wklej i uruchom całą zawartość `sekcja-41.sql`. Oczekiwane: „Success. No rows returned”.
    3. Kontrola: `select public.schema_marker_41();` → `true`.
    4. Upewnij się, że żaden otwarty panel admina nie jest w starej wersji (przeładuj karty panelu).
  </how-to-verify>
  <verify>
    <automated>npm run verify-prod</automated>
  </verify>
  <acceptance_criteria>
    - `npm run verify-prod` kod 0, w tym wiersz `schema_marker_41` = ok
    - Sonda podstawowa po wgraniu 41 kod 0 (SC5 OK)
  </acceptance_criteria>
  <resume-signal>Wpisz „41 wgrane” albo wklej błąd z SQL Editora</resume-signal>
  <done>Wycieki poprawności zamknięte dla starych i nowych klientów.</done>
</task>

</tasks>

<verification>
verify-prod (0) ze znacznikiem 41; sonda podstawowa (0).
</verification>

<success_criteria>
- Żaden RPC dostępny dla anona nie zwraca poprawności przed closes_at + 1,5 s
- Stare sygnatury nienaruszone
</success_criteria>

<output>
After completion, create `.planning/phases/06-rozgrywka-autorytatywna-serwera-i-plynnosc-jak-w-aplikacji/06-10-SUMMARY.md`
</output>
