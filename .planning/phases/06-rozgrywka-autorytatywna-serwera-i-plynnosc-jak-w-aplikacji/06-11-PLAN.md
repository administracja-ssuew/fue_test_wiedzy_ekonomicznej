---
phase: 06-rozgrywka-autorytatywna-serwera-i-plynnosc-jak-w-aplikacji
plan: 11
type: execute
wave: 8
depends_on: ["06-10"]
files_modified:
  - src/lib/gameLogic.js
  - src/lib/gameLogic.test.js
  - src/hooks/useLiveProjection.js
  - src/screens/AdminPanel.jsx
  - src/lib/supabase.js
autonomous: true
requirements: [P6-SC1, P6-SC4]

must_haves:
  truths:
    - "W bundlu nie ma żadnego kodu zapisującego przejście pytania z przeglądarki (kierowca admina, awaryjny kierowca uczestników)"
    - "Żaden ekran rozgrywki nie liczy czasu z ModulesContext (timePerQ tylko w edytorze modułów i auto-skip)"
    - "Testy, build i sonda podstawowa są zielone po usunięciu kodu"
  artifacts:
    - path: "src/lib/gameLogic.js"
      provides: "Bez shouldAdvance/advanceLeadSeconds/fallbackJitterMs/projectLiveState"
    - path: "src/screens/AdminPanel.jsx"
      provides: "Wyłącznie sterowanie v2"
    - path: "src/hooks/useLiveProjection.js"
      provides: "Wyłącznie projekcja z planu"
  key_links:
    - from: "src/screens/AdminPanel.jsx"
      to: "advanceSessionQuestion"
      via: "(usunięte)"
      pattern: "advanceSessionQuestion"
---

<objective>
Usunąć martwy po wdrożeniu kod legacy: decyzje kierowcy (`shouldAdvance`, `advanceLeadSeconds`, `fallbackJitterMs` + testy), gałęzie LEGACY w panelu (zapis przejść, stare pauza/wznowienie/Następne/Powtórz przez `upd`), projekcję z `q_started_at + modules` w `useLiveProjection`, nieużywane wrappery w `supabase.js`. Wykonywane po bramce 06-09 (`ZGODA NA UTWARDZENIE: TAK`).

Purpose: Decyzja CONTEXT „Kierowca w AdminPanel.jsx (driverTick) i awaryjny kierowca uczestników (fallbackJitterMs) do usunięcia po wdrożeniu”. Mniej kodu = zero ryzyka, że stara ścieżka ożyje.
Output: odchudzone `gameLogic.js` (+test), `useLiveProjection.js`, `AdminPanel.jsx`, `supabase.js`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-rozgrywka-autorytatywna-serwera-i-plynnosc-jak-w-aplikacji/06-09-DEPLOY.md
@.planning/phases/06-rozgrywka-autorytatywna-serwera-i-plynnosc-jak-w-aplikacji/06-06-SUMMARY.md

<interfaces>
Do usunięcia (po sprawdzeniu `grep -rn` — usuwaj eksport tylko, gdy nie ma już importów):
- src/lib/gameLogic.js: `shouldAdvance` (l.30), `advanceLeadSeconds` (l.37), `fallbackJitterMs` (l.104), `remainingSeconds` (l.119), `projectLiveState` (l.133). ZOSTAJĄ: `ANSWER_BG, ANSWER_LABELS, REVEAL_SECONDS, MODULE_INTRO_SECONDS, PRE_QUESTION_LEAD, earlySkipFloorSeconds, ANSWER_PLATEAU_MS, answerPlateauMs, AUTO_SKIP_MIN_TPQ, shouldEndEarly, cityInfo, getModule` (plan.js importuje stałe czasu).
- src/lib/gameLogic.test.js: testy powyższych (dziś l.153–214 dla kierowcy + testy projectLiveState/remainingSeconds, jeśli są).
- src/lib/supabase.js: `advanceSessionQuestion`, `startQuizSession`, `submitAnswer`, `getParticipantAnswers` (zostaje `saveAnswer` — używany przez DEMO `submitAnswerV2`; zostaje `updateSession` — „Ogłoś wyniki”/„Zakończ”).
- src/screens/AdminPanel.jsx: bloki oznaczone `// LEGACY (sesje bez planu) — usunąć w 06-11`, blok `// ── KIEROWCA PRZEJŚCIA PYTANIA` w `driverTick`, importy `shouldAdvance, advanceLeadSeconds, advanceSessionQuestion`.
- src/hooks/useLiveProjection.js: gałąź `projectLiveState` i fallback `mod?.timePerQ || 60`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: gameLogic + testy + useLiveProjection — usuń logikę kierowcy i projekcję legacy</name>
  <files>src/lib/gameLogic.js, src/lib/gameLogic.test.js, src/hooks/useLiveProjection.js</files>
  <read_first>
    - .planning/phases/06-rozgrywka-autorytatywna-serwera-i-plynnosc-jak-w-aplikacji/06-09-DEPLOY.md (MUSI zawierać `ZGODA NA UTWARDZENIE: TAK` — inaczej przerwij)
    - src/lib/gameLogic.js, src/lib/gameLogic.test.js (cały)
    - src/hooks/useLiveProjection.js (cały)
  </read_first>
  <action>
1. `gameLogic.js`: usuń `shouldAdvance`, `advanceLeadSeconds`, `fallbackJitterMs`, `remainingSeconds`, `projectLiveState` wraz z ich komentarzami (zostaw komentarz-sygnaturę: „Przejścia pytań wykonuje zamiatacz w bazie (faza 6, advance_due_sessions); projekcja: src/lib/plan.js”). Zaktualizuj komentarz przy `REVEAL_SECONDS` (już nie „advance_session_question”, tylko plan).
2. `gameLogic.test.js`: usuń bloki testów usuniętych funkcji; testy `shouldEndEarly`/`earlySkipFloorSeconds`/`answerPlateauMs` (w tym regresja 500/60) zostają.
3. `useLiveProjection.js`: usuń import i gałąź `projectLiveState`; sesja bez planu lub bez pobranego planu → `phase "waiting"` (bez licznika); `timePerQ = planTpq ?? 0`; usuń `useModules` tylko jeśli `mod` (nazwa/ikona/kolor dla LiveView) da się dalej wyznaczyć — `mod` ZOSTAJE z `MODULES.find(m => m.id === currentQ?.module)` (używany przez LiveView do nazwy/koloru), ale bez użycia `timePerQ`.
Przed usunięciem każdej funkcji: `grep -rn "<nazwa>" src scripts` — jeśli ma importerów poza usuwanymi miejscami, zostaw i odnotuj w SUMMARY.
  </action>
  <verify>
    <automated>npx vitest run && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "shouldAdvance\|advanceLeadSeconds\|fallbackJitterMs\|projectLiveState\|remainingSeconds" src` → 0 linii
    - `grep -n "timePerQ" src/hooks/useLiveProjection.js` → 0 linii poza kluczem zwracanym `timePerQ` przypisanym z `planTpq` (≤ 2 linie, każda zawiera `planTpq`)
    - `grep -c "shouldEndEarly" src/lib/gameLogic.test.js` ≥ 1 (regresja zachowana)
    - `npx vitest run` zielone; `npm run build` kod 0
  </acceptance_criteria>
  <done>Logika kierowcy i projekcja legacy usunięte; testy auto-skipu zachowane.</done>
</task>

<task type="auto">
  <name>Task 2: AdminPanel + supabase.js — usuń gałęzie LEGACY i nieużywane wrappery; testy + build + sonda podstawowa</name>
  <files>src/screens/AdminPanel.jsx, src/lib/supabase.js</files>
  <read_first>
    - src/screens/AdminPanel.jsx (driverTick, goToNextQuestion, przyciski — bloki `// LEGACY (sesje bez planu)`)
    - src/lib/supabase.js (advanceSessionQuestion, startQuizSession, submitAnswer, getParticipantAnswers)
  </read_first>
  <action>
1. `AdminPanel.jsx`: usuń blok `// ── KIEROWCA PRZEJŚCIA PYTANIA` (od komentarza do końca `try/finally` z `advanceSessionQuestion`) razem z linią `if (s.plan_anchor_at) return;`, refy używane wyłącznie przez niego (`advancingRef`, jeśli nieużywany gdzie indziej); gałęzie LEGACY przycisków: dla sesji bez planu w stanie running/paused pokaż zamiast przycisków sterowania informację (styl baneru ostrzegawczego): „Ta sesja została uruchomiona starszą wersją panelu — zakończ ją (⏹ Zakończ) i uruchom ponownie.” (przyciski „⏹ Zakończ” i „🔄 Odśwież” zostają). Usuń importy `shouldAdvance, advanceLeadSeconds, advanceSessionQuestion, startQuizSession`.
2. `supabase.js`: usuń `advanceSessionQuestion`, `startQuizSession`, `submitAnswer`, `getParticipantAnswers` — po `grep -rn` w `src` i `scripts` potwierdzającym brak importów (skrypty `bot-runner.js`/`load-runner.js` mogą importować — wtedy zostaw funkcję i odnotuj w SUMMARY).
3. Kontrola statyczna V-11: `grep -rn "timePerQ" src/App.jsx src/screens/Quiz.jsx src/hooks/useParticipantGame.js src/hooks/useLiveProjection.js` — dopuszczalne tylko przypisania z `planTpq`.
4. `npm test && npm run build && npm run verify-prod`; w tle `npx vite preview --port 4173 --strictPort`; `PROBE_TARGET=prod PROBE_CONFIRM=1 npm run sonda` oraz `PROBE_TARGET=prod PROBE_CONFIRM=1 PROBE_ADMIN_EXIT=1 npm run sonda` — oba kod 0. W SUMMARY przypomnienie dla użytkownika: po tym planie potrzebny jest kolejny deploy na Vercel (poza wydarzeniem).
  </action>
  <verify>
    <automated>npm test && npm run build && npm run verify-prod</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "advanceSessionQuestion\|advance_session_question\|armAdvanceFallback" src` → 0 linii
    - `grep -n "LEGACY (sesje bez planu)" src/screens/AdminPanel.jsx` → 0 linii
    - `grep -n "KIEROWCA PRZEJŚCIA PYTANIA" src/screens/AdminPanel.jsx` → 0 linii
    - `grep -n "uruchomiona starszą wersją panelu" src/screens/AdminPanel.jsx` → 1 linia
    - `npm test`, `npm run build`, `npm run verify-prod` kod 0; obie sondy kod 0 (wyniki w SUMMARY)
  </acceptance_criteria>
  <done>Kod rozgrywki istnieje w jednym modelu (plan + zamiatacz); legacy usunięte.</done>
</task>

</tasks>

<verification>
`npm test`, `npm run build`, `npm run verify-prod`, sonda podstawowa + ADMIN_EXIT — wszystko kod 0.
</verification>

<success_criteria>
- Brak kodu kierowcy w bundlu; brak timePerQ w ścieżkach rozgrywki
- Sondy zielone po sprzątaniu
</success_criteria>

<output>
After completion, create `.planning/phases/06-rozgrywka-autorytatywna-serwera-i-plynnosc-jak-w-aplikacji/06-11-SUMMARY.md`
</output>
